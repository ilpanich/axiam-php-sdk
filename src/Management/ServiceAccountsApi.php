<?php

declare(strict_types=1);

// Generated by scripts/gen_management.py from management-registry.json and openapi.json.
// DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.

namespace Axiam\Sdk\Management;

use Axiam\Sdk\Management\Models\BindCertificate;
use Axiam\Sdk\Management\Models\CreateServiceAccountRequest;
use Axiam\Sdk\Management\Models\RotateSecretResponse;
use Axiam\Sdk\Management\Models\ServiceAccountCreatedResponse;
use Axiam\Sdk\Management\Models\ServiceAccountResponse;
use Axiam\Sdk\Management\Models\UpdateServiceAccount;

/**
 * Machine identities, their secrets, and the certificate a device-bound one authenticates
 * with.
 *
 * The `service_accounts` namespace handle (CONTRACT.md §27.2), reached as
 * `$client->management()->serviceAccounts()`. Every method here goes through the one shared
 * {@see ManagementTransport}, so §3 CSRF, the §4 cookie jar, the §5 tenant header, §6 TLS, §16
 * retry and §19 telemetry apply without this class doing anything to opt in (§27.8).
 */
final class ServiceAccountsApi extends ManagementSupport
{
    /**
     * `GET /api/v1/service-accounts`
     *
     * `GET /api/v1/service-accounts`.
     *
     * Returns ONE page. `Page::$total` is the server's count across all pages and is not
     * `count($page)` — see §27.4 rule 4.
     * @param PageRequest|null $page which page to fetch; defaults to the first
     * @return Page<ServiceAccountResponse>
     */
    public function listItems(
        ?PageRequest $page = null,
    ): Page {
        return $this->transport->sendPage(
            'service_accounts.list',
            '/api/v1/service-accounts',
            [],
            [],
            $page ?? new PageRequest(),
            static fn (array $item): ServiceAccountResponse => ServiceAccountResponse::fromArray($item),
        );
    }

    /**
     * `POST /api/v1/service-accounts`
     *
     * `POST /api/v1/service-accounts`.
     *
     * The response carries a ONE-TIME secret (`client_secret`): the server will not return it
     * again, so a caller that does not persist it here cannot recover it (§27.5).
     * @param CreateServiceAccountRequest $body the request body
     * @return ServiceAccountCreatedResponse
     */
    public function create(
        CreateServiceAccountRequest $body,
    ): ServiceAccountCreatedResponse {
        $decoded = $this->transport->send(
            'service_accounts.create',
            'POST',
            '/api/v1/service-accounts',
            [],
            [],
            $body->toArray(),
        );

        return ServiceAccountCreatedResponse::fromArray($decoded ?? []);
    }

    /**
     * `GET /api/v1/service-accounts/{sa_id}`
     *
     * `GET /api/v1/service-accounts/{sa_id}`.
     * @param string $saId the `{sa_id}` path parameter
     * @return ServiceAccountResponse
     */
    public function get(
        string $saId,
    ): ServiceAccountResponse {
        $decoded = $this->transport->send(
            'service_accounts.get',
            'GET',
            '/api/v1/service-accounts/{sa_id}',
            ['sa_id' => $saId],
            [],
            null,
        );

        return ServiceAccountResponse::fromArray($decoded ?? []);
    }

    /**
     * `PUT /api/v1/service-accounts/{sa_id}`
     *
     * `PUT /api/v1/service-accounts/{sa_id}`.
     * @param string $saId the `{sa_id}` path parameter
     * @param UpdateServiceAccount $body the request body
     * @return ServiceAccountResponse
     */
    public function update(
        string $saId,
        UpdateServiceAccount $body,
    ): ServiceAccountResponse {
        $decoded = $this->transport->send(
            'service_accounts.update',
            'PUT',
            '/api/v1/service-accounts/{sa_id}',
            ['sa_id' => $saId],
            [],
            $body->toArray(),
        );

        return ServiceAccountResponse::fromArray($decoded ?? []);
    }

    /**
     * `DELETE /api/v1/service-accounts/{sa_id}`
     *
     * `DELETE /api/v1/service-accounts/{sa_id}`.
     *
     * Returns nothing; the server answers with an empty body.
     *
     * NOT idempotent (§27.4 rule 6): deleting something already deleted raises {@see
     * \Axiam\Sdk\Management\NotFoundError} rather than succeeding quietly.
     * @param string $saId the `{sa_id}` path parameter
     */
    public function delete(
        string $saId,
    ): void {
        $this->transport->send(
            'service_accounts.delete',
            'DELETE',
            '/api/v1/service-accounts/{sa_id}',
            ['sa_id' => $saId],
            [],
            null,
        );
    }

    /**
     * `POST /api/v1/service-accounts/{sa_id}/rotate-secret`
     *
     * `POST /api/v1/service-accounts/{sa_id}/rotate-secret`.
     *
     * The response carries a ONE-TIME secret (`client_secret`): the server will not return it
     * again, so a caller that does not persist it here cannot recover it (§27.5).
     * @param string $saId the `{sa_id}` path parameter
     * @return RotateSecretResponse
     */
    public function rotateSecret(
        string $saId,
    ): RotateSecretResponse {
        $decoded = $this->transport->send(
            'service_accounts.rotate_secret',
            'POST',
            '/api/v1/service-accounts/{sa_id}/rotate-secret',
            ['sa_id' => $saId],
            [],
            null,
        );

        return RotateSecretResponse::fromArray($decoded ?? []);
    }

    /**
     * `POST /api/v1/service-accounts/{sa_id}/bind-certificate`
     *
     * `POST /api/v1/service-accounts/{sa_id}/bind-certificate`.
     *
     * Returns nothing; the server answers with an empty body.
     * @param string $saId the `{sa_id}` path parameter
     * @param BindCertificate $body the request body
     */
    public function bindCertificate(
        string $saId,
        BindCertificate $body,
    ): void {
        $this->transport->send(
            'service_accounts.bind_certificate',
            'POST',
            '/api/v1/service-accounts/{sa_id}/bind-certificate',
            ['sa_id' => $saId],
            [],
            $body->toArray(),
        );
    }
}
