<?php

declare(strict_types=1);

// Generated by scripts/gen_management.py from management-registry.json and openapi.json.
// DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.

namespace Axiam\Sdk\Management;

use Axiam\Sdk\Management\Models\HealthResponse;
use Axiam\Sdk\Management\Models\MdsRefreshOutcome;
use Axiam\Sdk\Management\Models\MdsRefreshOutcomeVariant;
use Axiam\Sdk\Management\Models\MdsStatusResponse;
use Axiam\Sdk\Management\Models\ReadyResponse;

/**
 * Deployment-level probes and FIDO metadata state. Unauthenticated where the server leaves
 * them so.
 *
 * The `platform` namespace handle (CONTRACT.md §27.2), reached as
 * `$client->management()->platform()`. Every method here goes through the one shared {@see
 * ManagementTransport}, so §3 CSRF, the §4 cookie jar, the §5 tenant header, §6 TLS, §16 retry
 * and §19 telemetry apply without this class doing anything to opt in (§27.8).
 */
final class PlatformApi extends ManagementSupport
{
    /**
     * `GET /health` — liveness probe. Always returns 200.
     *
     * `GET /health`.
     * @return HealthResponse
     */
    public function health(): HealthResponse
    {
        $decoded = $this->transport->send(
            'platform.health',
            'GET',
            '/health',
            [],
            [],
            null,
        );

        return HealthResponse::fromArray($decoded ?? []);
    }

    /**
     * `GET /ready` — readiness probe. Checks DB connectivity.
     *
     * `GET /ready`.
     * @return ReadyResponse
     */
    public function ready(): ReadyResponse
    {
        $decoded = $this->transport->send(
            'platform.ready',
            'GET',
            '/ready',
            [],
            [],
            null,
        );

        return ReadyResponse::fromArray($decoded ?? []);
    }

    /**
     * `GET /api/v1/mds/status`
     *
     * `GET /api/v1/mds/status`.
     * @return MdsStatusResponse
     */
    public function mdsStatus(): MdsStatusResponse
    {
        $decoded = $this->transport->send(
            'platform.mds_status',
            'GET',
            '/api/v1/mds/status',
            [],
            [],
            null,
        );

        return MdsStatusResponse::fromArray($decoded ?? []);
    }

    /**
     * `POST /api/v1/mds/refresh`
     *
     * `POST /api/v1/mds/refresh`.
     * @return MdsRefreshOutcomeVariant
     */
    public function mdsRefresh(): MdsRefreshOutcomeVariant
    {
        $decoded = $this->transport->send(
            'platform.mds_refresh',
            'POST',
            '/api/v1/mds/refresh',
            [],
            [],
            null,
        );

        return MdsRefreshOutcome::fromArray($decoded ?? []);
    }
}
