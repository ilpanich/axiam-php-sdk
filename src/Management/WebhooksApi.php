<?php

declare(strict_types=1);

// Generated by scripts/gen_management.py from management-registry.json and openapi.json.
// DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.

namespace Axiam\Sdk\Management;

use Axiam\Sdk\Management\Models\CreateWebhookRequest;
use Axiam\Sdk\Management\Models\UpdateWebhookRequest;
use Axiam\Sdk\Management\Models\WebhookResponse;

/**
 * Outbound event notifications. Delivery signatures are verified with the §13 helper, which
 * this namespace configures.
 *
 * The `webhooks` namespace handle (CONTRACT.md §27.2), reached as
 * `$client->management()->webhooks()`. Every method here goes through the one shared {@see
 * ManagementTransport}, so §3 CSRF, the §4 cookie jar, the §5 tenant header, §6 TLS, §16 retry
 * and §19 telemetry apply without this class doing anything to opt in (§27.8).
 */
final class WebhooksApi extends ManagementSupport
{
    /**
     * `GET /api/v1/webhooks`
     *
     * `GET /api/v1/webhooks`.
     *
     * Returns ONE page. `Page::$total` is the server's count across all pages and is not
     * `count($page)` — see §27.4 rule 4.
     * @param PageRequest|null $page which page to fetch; defaults to the first
     * @return Page<WebhookResponse>
     */
    public function listItems(
        ?PageRequest $page = null,
    ): Page {
        return $this->transport->sendPage(
            'webhooks.list',
            '/api/v1/webhooks',
            [],
            [],
            $page ?? new PageRequest(),
            static fn (array $item): WebhookResponse => WebhookResponse::fromArray($item),
        );
    }

    /**
     * `POST /api/v1/webhooks`
     *
     * `POST /api/v1/webhooks`.
     * @param CreateWebhookRequest $body the request body
     * @return WebhookResponse
     */
    public function create(
        CreateWebhookRequest $body,
    ): WebhookResponse {
        $decoded = $this->transport->send(
            'webhooks.create',
            'POST',
            '/api/v1/webhooks',
            [],
            [],
            $body->toArray(),
        );

        return WebhookResponse::fromArray($decoded ?? []);
    }

    /**
     * `GET /api/v1/webhooks/{id}`
     *
     * `GET /api/v1/webhooks/{id}`.
     * @param string $id the `{id}` path parameter
     * @return WebhookResponse
     */
    public function get(
        string $id,
    ): WebhookResponse {
        $decoded = $this->transport->send(
            'webhooks.get',
            'GET',
            '/api/v1/webhooks/{id}',
            ['id' => $id],
            [],
            null,
        );

        return WebhookResponse::fromArray($decoded ?? []);
    }

    /**
     * `PUT /api/v1/webhooks/{id}`
     *
     * `PUT /api/v1/webhooks/{id}`.
     * @param string $id the `{id}` path parameter
     * @param UpdateWebhookRequest $body the request body
     * @return WebhookResponse
     */
    public function update(
        string $id,
        UpdateWebhookRequest $body,
    ): WebhookResponse {
        $decoded = $this->transport->send(
            'webhooks.update',
            'PUT',
            '/api/v1/webhooks/{id}',
            ['id' => $id],
            [],
            $body->toArray(),
        );

        return WebhookResponse::fromArray($decoded ?? []);
    }

    /**
     * `DELETE /api/v1/webhooks/{id}`
     *
     * `DELETE /api/v1/webhooks/{id}`.
     *
     * Returns nothing; the server answers with an empty body.
     *
     * NOT idempotent (§27.4 rule 6): deleting something already deleted raises {@see
     * \Axiam\Sdk\Management\NotFoundError} rather than succeeding quietly.
     * @param string $id the `{id}` path parameter
     */
    public function delete(
        string $id,
    ): void {
        $this->transport->send(
            'webhooks.delete',
            'DELETE',
            '/api/v1/webhooks/{id}',
            ['id' => $id],
            [],
            null,
        );
    }
}
