<?php

declare(strict_types=1);

// Generated by scripts/gen_management.py from management-registry.json and openapi.json.
// DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.

namespace Axiam\Sdk\Management;

use Axiam\Sdk\Management\Models\CreateResourceRequest;
use Axiam\Sdk\Management\Models\Resource;
use Axiam\Sdk\Management\Models\UpdateResourceRequest;

/**
 * The resource hierarchy role assignments cascade down.
 *
 * The `resources` namespace handle (CONTRACT.md §27.2), reached as
 * `$client->management()->resources()`. Every method here goes through the one shared {@see
 * ManagementTransport}, so §3 CSRF, the §4 cookie jar, the §5 tenant header, §6 TLS, §16 retry
 * and §19 telemetry apply without this class doing anything to opt in (§27.8).
 */
final class ResourcesApi extends ManagementSupport
{
    /**
     * `GET /api/v1/resources`
     *
     * `GET /api/v1/resources`.
     *
     * Returns ONE page. `Page::$total` is the server's count across all pages and is not
     * `count($page)` — see §27.4 rule 4.
     * @param PageRequest|null $page which page to fetch; defaults to the first
     * @return Page<Resource>
     */
    public function listItems(
        ?PageRequest $page = null,
    ): Page {
        return $this->transport->sendPage(
            'resources.list',
            '/api/v1/resources',
            [],
            [],
            $page ?? new PageRequest(),
            static fn (array $item): Resource => Resource::fromArray($item),
        );
    }

    /**
     * `POST /api/v1/resources`
     *
     * `POST /api/v1/resources`.
     * @param CreateResourceRequest $body the request body
     * @return Resource
     */
    public function create(
        CreateResourceRequest $body,
    ): Resource {
        $decoded = $this->transport->send(
            'resources.create',
            'POST',
            '/api/v1/resources',
            [],
            [],
            $body->toArray(),
        );

        return Resource::fromArray($decoded ?? []);
    }

    /**
     * `GET /api/v1/resources/{resource_id}`
     *
     * `GET /api/v1/resources/{resource_id}`.
     * @param string $resourceId the `{resource_id}` path parameter
     * @return Resource
     */
    public function get(
        string $resourceId,
    ): Resource {
        $decoded = $this->transport->send(
            'resources.get',
            'GET',
            '/api/v1/resources/{resource_id}',
            ['resource_id' => $resourceId],
            [],
            null,
        );

        return Resource::fromArray($decoded ?? []);
    }

    /**
     * `PUT /api/v1/resources/{resource_id}`
     *
     * `PUT /api/v1/resources/{resource_id}`.
     * @param string $resourceId the `{resource_id}` path parameter
     * @param UpdateResourceRequest $body the request body
     * @return Resource
     */
    public function update(
        string $resourceId,
        UpdateResourceRequest $body,
    ): Resource {
        $decoded = $this->transport->send(
            'resources.update',
            'PUT',
            '/api/v1/resources/{resource_id}',
            ['resource_id' => $resourceId],
            [],
            $body->toArray(),
        );

        return Resource::fromArray($decoded ?? []);
    }

    /**
     * `DELETE /api/v1/resources/{resource_id}`
     *
     * `DELETE /api/v1/resources/{resource_id}`.
     *
     * Returns nothing; the server answers with an empty body.
     *
     * NOT idempotent (§27.4 rule 6): deleting something already deleted raises {@see
     * \Axiam\Sdk\Management\NotFoundError} rather than succeeding quietly.
     * @param string $resourceId the `{resource_id}` path parameter
     */
    public function delete(
        string $resourceId,
    ): void {
        $this->transport->send(
            'resources.delete',
            'DELETE',
            '/api/v1/resources/{resource_id}',
            ['resource_id' => $resourceId],
            [],
            null,
        );
    }

    /**
     * `GET /api/v1/resources/{resource_id}/children`
     *
     * `GET /api/v1/resources/{resource_id}/children`.
     *
     * Returns the server's complete list. This endpoint is NOT paginated, so the result is a
     * plain list and never a `Page` (§27.4 rule 4).
     * @param string $resourceId the `{resource_id}` path parameter
     * @return list<Resource>
     */
    public function listChildren(
        string $resourceId,
    ): array {
        $decoded = $this->transport->send(
            'resources.list_children',
            'GET',
            '/api/v1/resources/{resource_id}/children',
            ['resource_id' => $resourceId],
            [],
            null,
        );

        $items = [];
        foreach ($decoded ?? [] as $item) {
            if (is_array($item)) {
                $items[] = Resource::fromArray($item);
            }
        }

        return $items;
    }

    /**
     * `GET /api/v1/resources/{resource_id}/ancestors`
     *
     * `GET /api/v1/resources/{resource_id}/ancestors`.
     *
     * Returns the server's complete list. This endpoint is NOT paginated, so the result is a
     * plain list and never a `Page` (§27.4 rule 4).
     * @param string $resourceId the `{resource_id}` path parameter
     * @return list<Resource>
     */
    public function listAncestors(
        string $resourceId,
    ): array {
        $decoded = $this->transport->send(
            'resources.list_ancestors',
            'GET',
            '/api/v1/resources/{resource_id}/ancestors',
            ['resource_id' => $resourceId],
            [],
            null,
        );

        $items = [];
        foreach ($decoded ?? [] as $item) {
            if (is_array($item)) {
                $items[] = Resource::fromArray($item);
            }
        }

        return $items;
    }
}
