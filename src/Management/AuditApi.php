<?php

declare(strict_types=1);

// Generated by scripts/gen_management.py from management-registry.json and openapi.json.
// DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.

namespace Axiam\Sdk\Management;

use Axiam\Sdk\Management\Models\AuditLogEntry;

/**
 * Append-only audit log, read-only by construction.
 *
 * The `audit` namespace handle (CONTRACT.md §27.2), reached as
 * `$client->management()->audit()`. Every method here goes through the one shared {@see
 * ManagementTransport}, so §3 CSRF, the §4 cookie jar, the §5 tenant header, §6 TLS, §16 retry
 * and §19 telemetry apply without this class doing anything to opt in (§27.8).
 */
final class AuditApi extends ManagementSupport
{
    /**
     * `GET /api/v1/audit-logs`
     *
     * `GET /api/v1/audit-logs`.
     *
     * Returns ONE page. `Page::$total` is the server's count across all pages and is not
     * `count($page)` — see §27.4 rule 4.
     * @param PageRequest|null $page which page to fetch; defaults to the first
     * @param string|null $actorId the optional `actor_id` query parameter
     * @param string|null $action the optional `action` query parameter
     * @param string|null $outcome the optional `outcome` query parameter
     * @param string|null $resourceId the optional `resource_id` query parameter
     * @param string|null $from the optional `from` query parameter
     * @param string|null $to the optional `to` query parameter
     * @return Page<AuditLogEntry>
     */
    public function listItems(
        ?PageRequest $page = null,
        ?string $actorId = null,
        ?string $action = null,
        ?string $outcome = null,
        ?string $resourceId = null,
        ?string $from = null,
        ?string $to = null,
    ): Page {
        return $this->transport->sendPage(
            'audit.list',
            '/api/v1/audit-logs',
            [],
            ['actor_id' => $actorId, 'action' => $action, 'outcome' => $outcome, 'resource_id' => $resourceId, 'from' => $from, 'to' => $to],
            $page ?? new PageRequest(),
            static fn (array $item): AuditLogEntry => AuditLogEntry::fromArray($item),
        );
    }

    /**
     * `GET /api/v1/audit-logs/system`
     *
     * `GET /api/v1/audit-logs/system`.
     *
     * Returns ONE page. `Page::$total` is the server's count across all pages and is not
     * `count($page)` — see §27.4 rule 4.
     * @param PageRequest|null $page which page to fetch; defaults to the first
     * @param string|null $actorId the optional `actor_id` query parameter
     * @param string|null $action the optional `action` query parameter
     * @param string|null $outcome the optional `outcome` query parameter
     * @param string|null $resourceId the optional `resource_id` query parameter
     * @param string|null $from the optional `from` query parameter
     * @param string|null $to the optional `to` query parameter
     * @return Page<AuditLogEntry>
     */
    public function listSystem(
        ?PageRequest $page = null,
        ?string $actorId = null,
        ?string $action = null,
        ?string $outcome = null,
        ?string $resourceId = null,
        ?string $from = null,
        ?string $to = null,
    ): Page {
        return $this->transport->sendPage(
            'audit.list_system',
            '/api/v1/audit-logs/system',
            [],
            ['actor_id' => $actorId, 'action' => $action, 'outcome' => $outcome, 'resource_id' => $resourceId, 'from' => $from, 'to' => $to],
            $page ?? new PageRequest(),
            static fn (array $item): AuditLogEntry => AuditLogEntry::fromArray($item),
        );
    }
}
