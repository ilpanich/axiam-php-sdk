<?php

declare(strict_types=1);

// Generated by scripts/gen_management.py from management-registry.json and openapi.json.
// DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.

namespace Axiam\Sdk\Management;

use Axiam\Sdk\Management\Models\CreateReactorRequest;
use Axiam\Sdk\Management\Models\ReactorEventDescriptor;
use Axiam\Sdk\Management\Models\ReactorResponse;
use Axiam\Sdk\Management\Models\UpdateReactorRequest;

/**
 * Registration of §22 AMQP extension actors -- the admin surface §22.9 describes, which no SDK
 * could previously reach.
 *
 * The `reactors` namespace handle (CONTRACT.md §27.2), reached as
 * `$client->management()->reactors()`. Every method here goes through the one shared {@see
 * ManagementTransport}, so §3 CSRF, the §4 cookie jar, the §5 tenant header, §6 TLS, §16 retry
 * and §19 telemetry apply without this class doing anything to opt in (§27.8).
 */
final class ReactorsApi extends ManagementSupport
{
    /**
     * `GET /api/v1/reactors`
     *
     * `GET /api/v1/reactors`.
     *
     * Returns ONE page. `Page::$total` is the server's count across all pages and is not
     * `count($page)` — see §27.4 rule 4.
     * @param PageRequest|null $page which page to fetch; defaults to the first
     * @return Page<ReactorResponse>
     */
    public function listItems(
        ?PageRequest $page = null,
    ): Page {
        return $this->transport->sendPage(
            'reactors.list',
            '/api/v1/reactors',
            [],
            [],
            $page ?? new PageRequest(),
            static fn (array $item): ReactorResponse => ReactorResponse::fromArray($item),
        );
    }

    /**
     * `POST /api/v1/reactors`
     *
     * `POST /api/v1/reactors`.
     * @param CreateReactorRequest $body the request body
     * @return ReactorResponse
     */
    public function create(
        CreateReactorRequest $body,
    ): ReactorResponse {
        $decoded = $this->transport->send(
            'reactors.create',
            'POST',
            '/api/v1/reactors',
            [],
            [],
            $body->toArray(),
        );

        return ReactorResponse::fromArray($decoded ?? []);
    }

    /**
     * `GET /api/v1/reactors/{id}`
     *
     * `GET /api/v1/reactors/{id}`.
     * @param string $id the `{id}` path parameter
     * @return ReactorResponse
     */
    public function get(
        string $id,
    ): ReactorResponse {
        $decoded = $this->transport->send(
            'reactors.get',
            'GET',
            '/api/v1/reactors/{id}',
            ['id' => $id],
            [],
            null,
        );

        return ReactorResponse::fromArray($decoded ?? []);
    }

    /**
     * `PUT /api/v1/reactors/{id}`
     *
     * `PUT /api/v1/reactors/{id}`.
     * @param string $id the `{id}` path parameter
     * @param UpdateReactorRequest $body the request body
     * @return ReactorResponse
     */
    public function update(
        string $id,
        UpdateReactorRequest $body,
    ): ReactorResponse {
        $decoded = $this->transport->send(
            'reactors.update',
            'PUT',
            '/api/v1/reactors/{id}',
            ['id' => $id],
            [],
            $body->toArray(),
        );

        return ReactorResponse::fromArray($decoded ?? []);
    }

    /**
     * `DELETE /api/v1/reactors/{id}`
     *
     * `DELETE /api/v1/reactors/{id}`.
     *
     * Returns nothing; the server answers with an empty body.
     *
     * NOT idempotent (§27.4 rule 6): deleting something already deleted raises {@see
     * \Axiam\Sdk\Management\NotFoundError} rather than succeeding quietly.
     * @param string $id the `{id}` path parameter
     */
    public function delete(
        string $id,
    ): void {
        $this->transport->send(
            'reactors.delete',
            'DELETE',
            '/api/v1/reactors/{id}',
            ['id' => $id],
            [],
            null,
        );
    }

    /**
     * `GET /api/v1/reactors/events` — the registry, verbatim.
     *
     * `GET /api/v1/reactors/events`.
     *
     * Returns the server's complete list. This endpoint is NOT paginated, so the result is a
     * plain list and never a `Page` (§27.4 rule 4).
     * @return list<ReactorEventDescriptor>
     */
    public function listEvents(): array
    {
        $decoded = $this->transport->send(
            'reactors.list_events',
            'GET',
            '/api/v1/reactors/events',
            [],
            [],
            null,
        );

        $items = [];
        foreach ($decoded ?? [] as $item) {
            if (is_array($item)) {
                $items[] = ReactorEventDescriptor::fromArray($item);
            }
        }

        return $items;
    }
}
