<?php

declare(strict_types=1);

// Generated by scripts/gen_management.py from management-registry.json and openapi.json.
// DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.

namespace Axiam\Sdk\Management\Models;

/**
 * Body of `POST /api/v1/organizations/{org_id}/ca-certificates/import`. Deliberately carries
 * no subject, validity window or key algorithm: all three are read out of the certificate
 * itself. A caller that could name them separately could name a subject the certificate does
 * not have, and AXIAM would enforce the claim while every relying party read the certificate.
 */
final class ImportCaCertificateRequest implements \JsonSerializable
{
    /**
     * Constructs a ImportCaCertificateRequest.
     * @param string $publicCertPem PEM-encoded CA certificate.
     * @param \Axiam\Sdk\Core\Sensitive|null $privateKeyPem PEM-encoded private key, if AXIAM
     *     is to take custody of it. Omit it to register the certificate as a trust anchor only.
     *     Write-only: it goes to the configured custodian on the way in and no endpoint returns
     *     it. (optional)
     */
    public function __construct(
        public readonly string $publicCertPem,
        public readonly ?\Axiam\Sdk\Core\Sensitive $privateKeyPem = null,
    ) {
    }

    /**
     * Rebuilds a ImportCaCertificateRequest from one decoded JSON object.
     * @param array<string,mixed> $data The raw wire object.
     */
    public static function fromArray(array $data): self
    {
        return new self(
            (string) ModelDecode::need($data, 'public_cert_pem', self::class),
            isset($data['private_key_pem']) ? new \Axiam\Sdk\Core\Sensitive((string) $data['private_key_pem']) : null,
        );
    }

    /**
     * Renders this object back to its wire form.
     *
     * §27.4 rule 5: a null property is OMITTED, not emitted as null. On a sparse update those
     * two say opposite things — "leave this alone" versus "set this to nothing" — and only
     * omission means the first.
     * @return array<string,mixed>
     */
    public function toArray(): array
    {
        $out = [];
        $out['public_cert_pem'] = $this->publicCertPem;
        if ($this->privateKeyPem !== null) {
            $out['private_key_pem'] = $this->privateKeyPem;
        }

        return $out;
    }

    /**
     * Renders this object for `json_encode()`.
     *
     * Any {@see \Axiam\Sdk\Core\Sensitive} it carries stays WRAPPED here, so a log line or a
     * `json_encode($model)` in application code prints `[SENSITIVE]`. The one place a secret
     * is revealed is {@see \Axiam\Sdk\Management\ManagementTransport}, on the way to the wire
     * and nowhere else (§27.5).
     * @return array<string,mixed>
     */
    public function jsonSerialize(): array
    {
        return $this->toArray();
    }
}
