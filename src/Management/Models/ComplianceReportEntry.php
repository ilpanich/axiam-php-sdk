<?php

declare(strict_types=1);

// Generated by scripts/gen_management.py from management-registry.json and openapi.json.
// DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.

namespace Axiam\Sdk\Management\Models;

/**
 * One credential's compliance outcome (D9).
 */
final class ComplianceReportEntry implements \JsonSerializable
{
    /**
     * Constructs a ComplianceReportEntry.
     * @param bool $compliant `false` only for a genuine policy violation
     *     ([`ComplianceStatus::NonCompliant`]) — a credential with no recorded AAGUID
     *     ([`ComplianceStatus::Unknown`], D9's pre-X3 case) is always `true` here, never reported
     *     as a violation.
     * @param string $credentialId the server's `credential_id` field
     * @param string $name The credential's user-assigned name (`WebauthnCredential::name`),
     *     not the owning user's account name.
     * @param string $userId the server's `user_id` field
     * @param string|null $aaguid the server's `aaguid` field (optional)
     * @param string|null $authenticatorName the server's `authenticator_name` field (optional)
     * @param string|null $reason `None` only when `compliant` and the credential has a
     *     recorded AAGUID; set for every non-compliant *and* every "unknown" (pre-X3) credential.
     *     (optional)
     */
    public function __construct(
        public readonly bool $compliant,
        public readonly string $credentialId,
        public readonly string $name,
        public readonly string $userId,
        public readonly ?string $aaguid = null,
        public readonly ?string $authenticatorName = null,
        public readonly ?string $reason = null,
    ) {
    }

    /**
     * Rebuilds a ComplianceReportEntry from one decoded JSON object.
     * @param array<string,mixed> $data The raw wire object.
     */
    public static function fromArray(array $data): self
    {
        return new self(
            (bool) $data['compliant'],
            (string) $data['credential_id'],
            (string) $data['name'],
            (string) $data['user_id'],
            isset($data['aaguid']) ? (string) $data['aaguid'] : null,
            isset($data['authenticator_name']) ? (string) $data['authenticator_name'] : null,
            isset($data['reason']) ? (string) $data['reason'] : null,
        );
    }

    /**
     * Renders this object back to its wire form.
     *
     * §27.4 rule 5: a null property is OMITTED, not emitted as null. On a sparse update those
     * two say opposite things — "leave this alone" versus "set this to nothing" — and only
     * omission means the first.
     * @return array<string,mixed>
     */
    public function toArray(): array
    {
        $out = [];
        $out['compliant'] = $this->compliant;
        $out['credential_id'] = $this->credentialId;
        $out['name'] = $this->name;
        $out['user_id'] = $this->userId;
        if ($this->aaguid !== null) {
            $out['aaguid'] = $this->aaguid;
        }
        if ($this->authenticatorName !== null) {
            $out['authenticator_name'] = $this->authenticatorName;
        }
        if ($this->reason !== null) {
            $out['reason'] = $this->reason;
        }

        return $out;
    }

    /**
     * Renders this object for `json_encode()`.
     *
     * Any {@see \Axiam\Sdk\Core\Sensitive} it carries stays WRAPPED here, so a log line or a
     * `json_encode($model)` in application code prints `[SENSITIVE]`. The one place a secret
     * is revealed is {@see \Axiam\Sdk\Management\ManagementTransport}, on the way to the wire
     * and nowhere else (§27.5).
     * @return array<string,mixed>
     */
    public function jsonSerialize(): array
    {
        return $this->toArray();
    }
}
