<?php

declare(strict_types=1);

// Generated by scripts/gen_management.py from management-registry.json and openapi.json.
// DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.

namespace Axiam\Sdk\Management\Models;

/**
 * One consent record, as the subject sees it.
 */
final class ConsentView implements \JsonSerializable
{
    /**
     * Constructs a ConsentView.
     * @param string $acceptedAt the server's `accepted_at` field
     * @param string $consentType What was consented to, e.g. `terms_of_service` or
     *     `oidc_scope_release:<client_id>`.
     * @param string $version The document version or, for a scope release, the consented
     *     scopes.
     * @param bool $withdrawable Whether this record can be withdrawn here. `false` for
     *     `terms_of_service`: withdrawing it is not a consent operation but an erasure, and it has
     *     its own endpoint with its own grace period. Reported rather than silently absent so the
     *     self-service page can show the record and explain it.
     */
    public function __construct(
        public readonly string $acceptedAt,
        public readonly string $consentType,
        public readonly string $version,
        public readonly bool $withdrawable,
    ) {
    }

    /**
     * Rebuilds a ConsentView from one decoded JSON object.
     * @param array<string,mixed> $data The raw wire object.
     */
    public static function fromArray(array $data): self
    {
        return new self(
            (string) ModelDecode::need($data, 'accepted_at', self::class),
            (string) ModelDecode::need($data, 'consent_type', self::class),
            (string) ModelDecode::need($data, 'version', self::class),
            (bool) ModelDecode::need($data, 'withdrawable', self::class),
        );
    }

    /**
     * Renders this object back to its wire form.
     *
     * §27.4 rule 5: a null property is OMITTED, not emitted as null. On a sparse update those
     * two say opposite things — "leave this alone" versus "set this to nothing" — and only
     * omission means the first.
     * @return array<string,mixed>
     */
    public function toArray(): array
    {
        $out = [];
        $out['accepted_at'] = $this->acceptedAt;
        $out['consent_type'] = $this->consentType;
        $out['version'] = $this->version;
        $out['withdrawable'] = $this->withdrawable;

        return $out;
    }

    /**
     * Renders this object for `json_encode()`.
     *
     * Any {@see \Axiam\Sdk\Core\Sensitive} it carries stays WRAPPED here, so a log line or a
     * `json_encode($model)` in application code prints `[SENSITIVE]`. The one place a secret
     * is revealed is {@see \Axiam\Sdk\Management\ManagementTransport}, on the way to the wire
     * and nowhere else (§27.5).
     * @return array<string,mixed>
     */
    public function jsonSerialize(): array
    {
        return $this->toArray();
    }
}
