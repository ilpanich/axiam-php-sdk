<?php

declare(strict_types=1);

// Generated by scripts/gen_management.py from management-registry.json and openapi.json.
// DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.

namespace Axiam\Sdk\Management\Models;

/**
 * `GET /api/v1/mds/status` response. `no`/`next_update`/`last_refreshed_at` are `None` and
 * `stale` is `false` when MDS has never been ingested — a meaningful, valid answer ("nothing
 * ingested yet"), not an error.
 */
final class MdsStatusResponse implements \JsonSerializable
{
    /**
     * Constructs a MdsStatusResponse.
     * @param int $entryCount the server's `entry_count` field
     * @param bool $stale the server's `stale` field
     * @param string|null $lastRefreshedAt the server's `last_refreshed_at` field (optional)
     * @param string|null $nextUpdate the server's `next_update` field (optional)
     * @param int|null $no the server's `no` field (optional)
     */
    public function __construct(
        public readonly int $entryCount,
        public readonly bool $stale,
        public readonly ?string $lastRefreshedAt = null,
        public readonly ?string $nextUpdate = null,
        public readonly ?int $no = null,
    ) {
    }

    /**
     * Rebuilds a MdsStatusResponse from one decoded JSON object.
     * @param array<string,mixed> $data The raw wire object.
     */
    public static function fromArray(array $data): self
    {
        return new self(
            (int) $data['entry_count'],
            (bool) $data['stale'],
            isset($data['last_refreshed_at']) ? (string) $data['last_refreshed_at'] : null,
            isset($data['next_update']) ? (string) $data['next_update'] : null,
            isset($data['no']) ? (int) $data['no'] : null,
        );
    }

    /**
     * Renders this object back to its wire form.
     *
     * §27.4 rule 5: a null property is OMITTED, not emitted as null. On a sparse update those
     * two say opposite things — "leave this alone" versus "set this to nothing" — and only
     * omission means the first.
     * @return array<string,mixed>
     */
    public function toArray(): array
    {
        $out = [];
        $out['entry_count'] = $this->entryCount;
        $out['stale'] = $this->stale;
        if ($this->lastRefreshedAt !== null) {
            $out['last_refreshed_at'] = $this->lastRefreshedAt;
        }
        if ($this->nextUpdate !== null) {
            $out['next_update'] = $this->nextUpdate;
        }
        if ($this->no !== null) {
            $out['no'] = $this->no;
        }

        return $out;
    }

    /**
     * Renders this object for `json_encode()`.
     *
     * Any {@see \Axiam\Sdk\Core\Sensitive} it carries stays WRAPPED here, so a log line or a
     * `json_encode($model)` in application code prints `[SENSITIVE]`. The one place a secret
     * is revealed is {@see \Axiam\Sdk\Management\ManagementTransport}, on the way to the wire
     * and nowhere else (§27.5).
     * @return array<string,mixed>
     */
    public function jsonSerialize(): array
    {
        return $this->toArray();
    }
}
