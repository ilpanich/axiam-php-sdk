<?php

declare(strict_types=1);

// Generated by scripts/gen_management.py from management-registry.json and openapi.json.
// DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.

namespace Axiam\Sdk\Management\Models;

/**
 * One of a user's sessions, as an administrator sees it.
 */
final class SessionResponse implements \JsonSerializable
{
    /**
     * Constructs a SessionResponse.
     * @param list<string> $amr RFC 8176 method references for that authentication.
     * @param string $authenticatedAt X7.2 — when the end user actually authenticated, which is
     *     not `created_at` on a session produced by refresh rotation.
     * @param string $createdAt the server's `created_at` field
     * @param string $expiresAt the server's `expires_at` field
     * @param string $id the server's `id` field
     * @param int $refreshReplayGraceAccepted T-254 — replays accepted under the FAPI 2.0
     *     §5.3.2.1-9 grace window. Only ever non-zero for a client registered `profile: fapi2`.
     * @param int $refreshReplayRefused T-254 — replays refused because there was no window to
     *     accept them in. Nothing a conformant client does.
     * @param string $refreshReplayVerdict T-254 — the badge: `none`, `fapi_grace_retry` or
     *     `refused`. Derived from the two counters below rather than stored, so it cannot disagree
     *     with them. A refusal outranks an accepted grace retry however the counts compare.
     * @param string|null $ipAddress the server's `ip_address` field (optional)
     * @param string|null $refreshReplayAt T-254 — when a refresh token of this session was
     *     last presented after it had already been rotated. `None` if that has never happened.
     *     (optional)
     * @param string|null $userAgent the server's `user_agent` field (optional)
     */
    public function __construct(
        public readonly array $amr,
        public readonly string $authenticatedAt,
        public readonly string $createdAt,
        public readonly string $expiresAt,
        public readonly string $id,
        public readonly int $refreshReplayGraceAccepted,
        public readonly int $refreshReplayRefused,
        public readonly string $refreshReplayVerdict,
        public readonly ?string $ipAddress = null,
        public readonly ?string $refreshReplayAt = null,
        public readonly ?string $userAgent = null,
    ) {
    }

    /**
     * Rebuilds a SessionResponse from one decoded JSON object.
     * @param array<string,mixed> $data The raw wire object.
     */
    public static function fromArray(array $data): self
    {
        return new self(
            array_values(array_map(static fn (mixed $v): string => (string) $v, (array) ModelDecode::need($data, 'amr', self::class))),
            (string) ModelDecode::need($data, 'authenticated_at', self::class),
            (string) ModelDecode::need($data, 'created_at', self::class),
            (string) ModelDecode::need($data, 'expires_at', self::class),
            (string) ModelDecode::need($data, 'id', self::class),
            (int) ModelDecode::need($data, 'refresh_replay_grace_accepted', self::class),
            (int) ModelDecode::need($data, 'refresh_replay_refused', self::class),
            (string) ModelDecode::need($data, 'refresh_replay_verdict', self::class),
            isset($data['ip_address']) ? (string) $data['ip_address'] : null,
            isset($data['refresh_replay_at']) ? (string) $data['refresh_replay_at'] : null,
            isset($data['user_agent']) ? (string) $data['user_agent'] : null,
        );
    }

    /**
     * Renders this object back to its wire form.
     *
     * §27.4 rule 5: a null property is OMITTED, not emitted as null. On a sparse update those
     * two say opposite things — "leave this alone" versus "set this to nothing" — and only
     * omission means the first.
     * @return array<string,mixed>
     */
    public function toArray(): array
    {
        $out = [];
        $out['amr'] = $this->amr;
        $out['authenticated_at'] = $this->authenticatedAt;
        $out['created_at'] = $this->createdAt;
        $out['expires_at'] = $this->expiresAt;
        $out['id'] = $this->id;
        $out['refresh_replay_grace_accepted'] = $this->refreshReplayGraceAccepted;
        $out['refresh_replay_refused'] = $this->refreshReplayRefused;
        $out['refresh_replay_verdict'] = $this->refreshReplayVerdict;
        if ($this->ipAddress !== null) {
            $out['ip_address'] = $this->ipAddress;
        }
        if ($this->refreshReplayAt !== null) {
            $out['refresh_replay_at'] = $this->refreshReplayAt;
        }
        if ($this->userAgent !== null) {
            $out['user_agent'] = $this->userAgent;
        }

        return $out;
    }

    /**
     * Renders this object for `json_encode()`.
     *
     * Any {@see \Axiam\Sdk\Core\Sensitive} it carries stays WRAPPED here, so a log line or a
     * `json_encode($model)` in application code prints `[SENSITIVE]`. The one place a secret
     * is revealed is {@see \Axiam\Sdk\Management\ManagementTransport}, on the way to the wire
     * and nowhere else (§27.5).
     * @return array<string,mixed>
     */
    public function jsonSerialize(): array
    {
        return $this->toArray();
    }
}
