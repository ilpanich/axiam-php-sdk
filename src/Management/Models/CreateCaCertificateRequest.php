<?php

declare(strict_types=1);

// Generated by scripts/gen_management.py from management-registry.json and openapi.json.
// DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.

namespace Axiam\Sdk\Management\Models;

/**
 * The `CreateCaCertificateRequest` schema from the server's OpenAPI document.
 */
final class CreateCaCertificateRequest implements \JsonSerializable
{
    /**
     * Constructs a CreateCaCertificateRequest.
     * @param KeyAlgorithm $keyAlgorithm the server's `key_algorithm` field
     * @param string $subject the server's `subject` field
     * @param int $validityDays Validity duration in days.
     * @param string|null $intermediateSubject Common name for the signing intermediate —
     *     `vault_pki` custody only. Under that custodian Vault generates a root and an
     *     intermediate beneath it, and this names the second. Defaults to the root's subject with
     *     `Intermediate Authority` appended. Ignored by every other custodian, which produces one
     *     self-signed CA and has no second certificate to name. (optional)
     * @param int|null $intermediateValidityDays Validity of the signing intermediate, in days.
     *     Defaults to the root's. (optional)
     * @param bool|null $issueFromRoot Issue leaves straight from the generated root instead of
     *     creating an intermediate. `vault_pki` custody only, and off by default. The default is
     *     the safer one: a root that signs only an intermediate can have that intermediate revoked
     *     and replaced without redistributing the trust anchor, and a root that signs leaves
     *     cannot. (optional)
     */
    public function __construct(
        public readonly KeyAlgorithm $keyAlgorithm,
        public readonly string $subject,
        public readonly int $validityDays,
        public readonly ?string $intermediateSubject = null,
        public readonly ?int $intermediateValidityDays = null,
        public readonly ?bool $issueFromRoot = null,
    ) {
    }

    /**
     * Rebuilds a CreateCaCertificateRequest from one decoded JSON object.
     * @param array<string,mixed> $data The raw wire object.
     */
    public static function fromArray(array $data): self
    {
        return new self(
            KeyAlgorithm::fromWire((string) $data['key_algorithm']),
            (string) $data['subject'],
            (int) $data['validity_days'],
            isset($data['intermediate_subject']) ? (string) $data['intermediate_subject'] : null,
            isset($data['intermediate_validity_days']) ? (int) $data['intermediate_validity_days'] : null,
            isset($data['issue_from_root']) ? (bool) $data['issue_from_root'] : null,
        );
    }

    /**
     * Renders this object back to its wire form.
     *
     * §27.4 rule 5: a null property is OMITTED, not emitted as null. On a sparse update those
     * two say opposite things — "leave this alone" versus "set this to nothing" — and only
     * omission means the first.
     * @return array<string,mixed>
     */
    public function toArray(): array
    {
        $out = [];
        $out['key_algorithm'] = $this->keyAlgorithm->value;
        $out['subject'] = $this->subject;
        $out['validity_days'] = $this->validityDays;
        if ($this->intermediateSubject !== null) {
            $out['intermediate_subject'] = $this->intermediateSubject;
        }
        if ($this->intermediateValidityDays !== null) {
            $out['intermediate_validity_days'] = $this->intermediateValidityDays;
        }
        if ($this->issueFromRoot !== null) {
            $out['issue_from_root'] = $this->issueFromRoot;
        }

        return $out;
    }

    /**
     * Renders this object for `json_encode()`.
     *
     * Any {@see \Axiam\Sdk\Core\Sensitive} it carries stays WRAPPED here, so a log line or a
     * `json_encode($model)` in application code prints `[SENSITIVE]`. The one place a secret
     * is revealed is {@see \Axiam\Sdk\Management\ManagementTransport}, on the way to the wire
     * and nowhere else (§27.5).
     * @return array<string,mixed>
     */
    public function jsonSerialize(): array
    {
        return $this->toArray();
    }
}
