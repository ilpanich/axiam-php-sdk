<?php

declare(strict_types=1);

// Generated by scripts/gen_management.py from management-registry.json and openapi.json.
// DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.

namespace Axiam\Sdk\Management\Models;

/**
 * A permission grant with its scopes resolved. A superset of [`PermissionGrant`]: `scope_ids`
 * is still present and still authoritative, so a client written before `scopes` existed is
 * unaffected.
 */
final class ResolvedPermissionGrant implements \JsonSerializable
{
    /**
     * Constructs a ResolvedPermissionGrant.
     * @param PermissionEffect $effect Whether the grant permits or refuses.
     * @param Permission $permission The permission this grant is about.
     * @param list<string> $scopeIds The scope ids the grant is constrained to. Empty is the
     *     wildcard.
     * @param list<GrantedScope> $scopes The scopes `scope_ids` names, in the same order where
     *     resolvable. A scope that cannot be resolved is **omitted** rather than represented by a
     *     placeholder, so this can be shorter than `scope_ids`. That happens when a scope was
     *     deleted while a grant still referenced it, and showing a name for something that no
     *     longer exists would be worse than showing one fewer chip beside a count that still says
     *     how many ids the grant carries.
     */
    public function __construct(
        public readonly PermissionEffect $effect,
        public readonly Permission $permission,
        public readonly array $scopeIds,
        public readonly array $scopes,
    ) {
    }

    /**
     * Rebuilds a ResolvedPermissionGrant from one decoded JSON object.
     * @param array<string,mixed> $data The raw wire object.
     */
    public static function fromArray(array $data): self
    {
        return new self(
            PermissionEffect::fromWire((string) $data['effect']),
            Permission::fromArray((array) $data['permission']),
            array_values(array_map(static fn (mixed $v): string => (string) $v, (array) $data['scope_ids'])),
            array_values(array_map(static fn (mixed $v): GrantedScope => GrantedScope::fromArray((array) $v), (array) $data['scopes'])),
        );
    }

    /**
     * Renders this object back to its wire form.
     *
     * §27.4 rule 5: a null property is OMITTED, not emitted as null. On a sparse update those
     * two say opposite things — "leave this alone" versus "set this to nothing" — and only
     * omission means the first.
     * @return array<string,mixed>
     */
    public function toArray(): array
    {
        $out = [];
        $out['effect'] = $this->effect->value;
        $out['permission'] = $this->permission->toArray();
        $out['scope_ids'] = $this->scopeIds;
        $out['scopes'] = array_map(static fn (GrantedScope $v): array => $v->toArray(), $this->scopes);

        return $out;
    }

    /**
     * Renders this object for `json_encode()`.
     *
     * Any {@see \Axiam\Sdk\Core\Sensitive} it carries stays WRAPPED here, so a log line or a
     * `json_encode($model)` in application code prints `[SENSITIVE]`. The one place a secret
     * is revealed is {@see \Axiam\Sdk\Management\ManagementTransport}, on the way to the wire
     * and nowhere else (§27.5).
     * @return array<string,mixed>
     */
    public function jsonSerialize(): array
    {
        return $this->toArray();
    }
}
