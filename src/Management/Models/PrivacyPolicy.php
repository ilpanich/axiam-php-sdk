<?php

declare(strict_types=1);

// Generated by scripts/gen_management.py from management-registry.json and openapi.json.
// DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.

namespace Axiam\Sdk\Management\Models;

/**
 * Data-retention rules that apply after a subject asks to be erased.
 */
final class PrivacyPolicy implements \JsonSerializable
{
    /**
     * Constructs a PrivacyPolicy.
     * @param int $deletionGracePeriodDays How long a requested account erasure stays
     *     cancellable before the purge runs, in days. The window exists so an erasure triggered by
     *     mistake, or under coercion, can be undone — `POST /api/v1/auth/account/delete/cancel`
     *     works for exactly this long. It was fixed at 30 days in the handler, which meant the
     *     "cancel a pending deletion" control in the admin UI referred to a duration no operator
     *     could see or change. Shorter is the more restrictive direction, so a tenant may lower it
     *     and not raise it: it is time spent holding data the subject has already asked to have
     *     erased, and GDPR Art. 17(1) asks for that to be "without undue delay". The upper bound
     *     of 90 days is where Art. 12(3)'s one-month response deadline plus its two-month
     *     extension for complex cases runs out; anything past 30 wants a reason recorded.
     */
    public function __construct(
        public readonly int $deletionGracePeriodDays,
    ) {
    }

    /**
     * Rebuilds a PrivacyPolicy from one decoded JSON object.
     * @param array<string,mixed> $data The raw wire object.
     */
    public static function fromArray(array $data): self
    {
        return new self(
            (int) $data['deletion_grace_period_days'],
        );
    }

    /**
     * Renders this object back to its wire form.
     *
     * §27.4 rule 5: a null property is OMITTED, not emitted as null. On a sparse update those
     * two say opposite things — "leave this alone" versus "set this to nothing" — and only
     * omission means the first.
     * @return array<string,mixed>
     */
    public function toArray(): array
    {
        $out = [];
        $out['deletion_grace_period_days'] = $this->deletionGracePeriodDays;

        return $out;
    }

    /**
     * Renders this object for `json_encode()`.
     *
     * Any {@see \Axiam\Sdk\Core\Sensitive} it carries stays WRAPPED here, so a log line or a
     * `json_encode($model)` in application code prints `[SENSITIVE]`. The one place a secret
     * is revealed is {@see \Axiam\Sdk\Management\ManagementTransport}, on the way to the wire
     * and nowhere else (§27.5).
     * @return array<string,mixed>
     */
    public function jsonSerialize(): array
    {
        return $this->toArray();
    }
}
