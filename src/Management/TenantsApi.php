<?php

declare(strict_types=1);

// Generated by scripts/gen_management.py from management-registry.json and openapi.json.
// DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.

namespace Axiam\Sdk\Management;

use Axiam\Sdk\Management\Models\CreateTenantRequest;
use Axiam\Sdk\Management\Models\Tenant;
use Axiam\Sdk\Management\Models\UpdateTenant;

/**
 * Tenants within an organization -- the isolation boundary every other namespace is scoped to.
 *
 * The `tenants` namespace handle (CONTRACT.md §27.2), reached as
 * `$client->management()->tenants()`. Every method here goes through the one shared {@see
 * ManagementTransport}, so §3 CSRF, the §4 cookie jar, the §5 tenant header, §6 TLS, §16 retry
 * and §19 telemetry apply without this class doing anything to opt in (§27.8).
 */
final class TenantsApi extends ManagementSupport
{
    /**
     * `GET /api/v1/organizations/{org_id}/tenants`
     *
     * `GET /api/v1/organizations/{org_id}/tenants`.
     *
     * Returns ONE page. `Page::$total` is the server's count across all pages and is not
     * `count($page)` — see §27.4 rule 4.
     * @param PageRequest|null $page which page to fetch; defaults to the first
     * @return Page<Tenant>
     */
    public function listItems(
        ?PageRequest $page = null,
    ): Page {
        return $this->transport->sendPage(
            'tenants.list',
            '/api/v1/organizations/{org_id}/tenants',
            ['org_id' => $this->orgId()],
            [],
            $page ?? new PageRequest(),
            static fn (array $item): Tenant => Tenant::fromArray($item),
        );
    }

    /**
     * `POST /api/v1/organizations/{org_id}/tenants`
     *
     * `POST /api/v1/organizations/{org_id}/tenants`.
     * @param CreateTenantRequest $body the request body
     * @return Tenant
     */
    public function create(
        CreateTenantRequest $body,
    ): Tenant {
        $decoded = $this->transport->send(
            'tenants.create',
            'POST',
            '/api/v1/organizations/{org_id}/tenants',
            ['org_id' => $this->orgId()],
            [],
            $body->toArray(),
        );

        return Tenant::fromArray($decoded ?? []);
    }

    /**
     * `GET /api/v1/organizations/{org_id}/tenants/{tenant_id}`
     *
     * `GET /api/v1/organizations/{org_id}/tenants/{tenant_id}`.
     * @param string $tenantId the `{tenant_id}` path parameter
     * @return Tenant
     */
    public function get(
        string $tenantId,
    ): Tenant {
        $decoded = $this->transport->send(
            'tenants.get',
            'GET',
            '/api/v1/organizations/{org_id}/tenants/{tenant_id}',
            ['org_id' => $this->orgId(), 'tenant_id' => $tenantId],
            [],
            null,
        );

        return Tenant::fromArray($decoded ?? []);
    }

    /**
     * `PUT /api/v1/organizations/{org_id}/tenants/{tenant_id}`
     *
     * `PUT /api/v1/organizations/{org_id}/tenants/{tenant_id}`.
     * @param string $tenantId the `{tenant_id}` path parameter
     * @param UpdateTenant $body the request body
     * @return Tenant
     */
    public function update(
        string $tenantId,
        UpdateTenant $body,
    ): Tenant {
        $decoded = $this->transport->send(
            'tenants.update',
            'PUT',
            '/api/v1/organizations/{org_id}/tenants/{tenant_id}',
            ['org_id' => $this->orgId(), 'tenant_id' => $tenantId],
            [],
            $body->toArray(),
        );

        return Tenant::fromArray($decoded ?? []);
    }

    /**
     * `DELETE /api/v1/organizations/{org_id}/tenants/{tenant_id}`
     *
     * `DELETE /api/v1/organizations/{org_id}/tenants/{tenant_id}`.
     *
     * Returns nothing; the server answers with an empty body.
     *
     * NOT idempotent (§27.4 rule 6): deleting something already deleted raises {@see
     * \Axiam\Sdk\Management\NotFoundError} rather than succeeding quietly.
     * @param string $tenantId the `{tenant_id}` path parameter
     */
    public function delete(
        string $tenantId,
    ): void {
        $this->transport->send(
            'tenants.delete',
            'DELETE',
            '/api/v1/organizations/{org_id}/tenants/{tenant_id}',
            ['org_id' => $this->orgId(), 'tenant_id' => $tenantId],
            [],
            null,
        );
    }
}
