<?php

declare(strict_types=1);

// Generated by scripts/gen_management.py from management-registry.json and openapi.json.
// DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.

namespace Axiam\Sdk\Management;

use Axiam\Sdk\Management\Models\CreateScopeRequest;
use Axiam\Sdk\Management\Models\Scope;
use Axiam\Sdk\Management\Models\UpdateScopeRequest;

/**
 * Sub-resource granularity, always addressed under their resource.
 *
 * The `scopes` namespace handle (CONTRACT.md §27.2), reached as
 * `$client->management()->scopes()`. Every method here goes through the one shared {@see
 * ManagementTransport}, so §3 CSRF, the §4 cookie jar, the §5 tenant header, §6 TLS, §16 retry
 * and §19 telemetry apply without this class doing anything to opt in (§27.8).
 */
final class ScopesApi extends ManagementSupport
{
    /**
     * `GET /api/v1/resources/{resource_id}/scopes`
     *
     * `GET /api/v1/resources/{resource_id}/scopes`.
     *
     * Returns the server's complete list. This endpoint is NOT paginated, so the result is a
     * plain list and never a `Page` (§27.4 rule 4).
     * @param string $resourceId the `{resource_id}` path parameter
     * @return list<Scope>
     */
    public function listItems(
        string $resourceId,
    ): array {
        $decoded = $this->transport->send(
            'scopes.list',
            'GET',
            '/api/v1/resources/{resource_id}/scopes',
            ['resource_id' => $resourceId],
            [],
            null,
        );

        $items = [];
        foreach ($decoded ?? [] as $item) {
            if (is_array($item)) {
                $items[] = Scope::fromArray($item);
            }
        }

        return $items;
    }

    /**
     * `POST /api/v1/resources/{resource_id}/scopes`
     *
     * `POST /api/v1/resources/{resource_id}/scopes`.
     * @param string $resourceId the `{resource_id}` path parameter
     * @param CreateScopeRequest $body the request body
     * @return Scope
     */
    public function create(
        string $resourceId,
        CreateScopeRequest $body,
    ): Scope {
        $decoded = $this->transport->send(
            'scopes.create',
            'POST',
            '/api/v1/resources/{resource_id}/scopes',
            ['resource_id' => $resourceId],
            [],
            $body->toArray(),
        );

        return Scope::fromArray($decoded ?? []);
    }

    /**
     * `GET /api/v1/resources/{resource_id}/scopes/{scope_id}`
     *
     * `GET /api/v1/resources/{resource_id}/scopes/{scope_id}`.
     * @param string $resourceId the `{resource_id}` path parameter
     * @param string $scopeId the `{scope_id}` path parameter
     * @return Scope
     */
    public function get(
        string $resourceId,
        string $scopeId,
    ): Scope {
        $decoded = $this->transport->send(
            'scopes.get',
            'GET',
            '/api/v1/resources/{resource_id}/scopes/{scope_id}',
            ['resource_id' => $resourceId, 'scope_id' => $scopeId],
            [],
            null,
        );

        return Scope::fromArray($decoded ?? []);
    }

    /**
     * `PUT /api/v1/resources/{resource_id}/scopes/{scope_id}`
     *
     * `PUT /api/v1/resources/{resource_id}/scopes/{scope_id}`.
     * @param string $resourceId the `{resource_id}` path parameter
     * @param string $scopeId the `{scope_id}` path parameter
     * @param UpdateScopeRequest $body the request body
     * @return Scope
     */
    public function update(
        string $resourceId,
        string $scopeId,
        UpdateScopeRequest $body,
    ): Scope {
        $decoded = $this->transport->send(
            'scopes.update',
            'PUT',
            '/api/v1/resources/{resource_id}/scopes/{scope_id}',
            ['resource_id' => $resourceId, 'scope_id' => $scopeId],
            [],
            $body->toArray(),
        );

        return Scope::fromArray($decoded ?? []);
    }

    /**
     * `DELETE /api/v1/resources/{resource_id}/scopes/{scope_id}`
     *
     * `DELETE /api/v1/resources/{resource_id}/scopes/{scope_id}`.
     *
     * Returns nothing; the server answers with an empty body.
     *
     * NOT idempotent (§27.4 rule 6): deleting something already deleted raises {@see
     * \Axiam\Sdk\Management\NotFoundError} rather than succeeding quietly.
     * @param string $resourceId the `{resource_id}` path parameter
     * @param string $scopeId the `{scope_id}` path parameter
     */
    public function delete(
        string $resourceId,
        string $scopeId,
    ): void {
        $this->transport->send(
            'scopes.delete',
            'DELETE',
            '/api/v1/resources/{resource_id}/scopes/{scope_id}',
            ['resource_id' => $resourceId, 'scope_id' => $scopeId],
            [],
            null,
        );
    }
}
