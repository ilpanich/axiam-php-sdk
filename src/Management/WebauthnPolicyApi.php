<?php

declare(strict_types=1);

// Generated by scripts/gen_management.py from management-registry.json and openapi.json.
// DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.

namespace Axiam\Sdk\Management;

use Axiam\Sdk\Management\Models\ComplianceReportEntry;
use Axiam\Sdk\Management\Models\PolicyResponse;
use Axiam\Sdk\Management\Models\WebauthnAttestationPolicy;

/**
 * Per-tenant attestation policy governing the §24 ceremonies, and the compliance report over
 * it.
 *
 * The `webauthn_policy` namespace handle (CONTRACT.md §27.2), reached as
 * `$client->management()->webauthnPolicy()`. Every method here goes through the one shared
 * {@see ManagementTransport}, so §3 CSRF, the §4 cookie jar, the §5 tenant header, §6 TLS, §16
 * retry and §19 telemetry apply without this class doing anything to opt in (§27.8).
 */
final class WebauthnPolicyApi extends ManagementSupport
{
    /**
     * `GET /api/v1/tenants/{tenant_id}/webauthn/attestation-policy`
     *
     * `GET /api/v1/tenants/{tenant_id}/webauthn/attestation-policy`.
     * @return PolicyResponse
     */
    public function get(): PolicyResponse
    {
        $decoded = $this->transport->send(
            'webauthn_policy.get',
            'GET',
            '/api/v1/tenants/{tenant_id}/webauthn/attestation-policy',
            ['tenant_id' => $this->tenantId()],
            [],
            null,
        );

        return PolicyResponse::fromArray($decoded ?? []);
    }

    /**
     * `PUT /api/v1/tenants/{tenant_id}/webauthn/attestation-policy`
     *
     * `PUT /api/v1/tenants/{tenant_id}/webauthn/attestation-policy`.
     * @param WebauthnAttestationPolicy $body the request body
     * @return WebauthnAttestationPolicy
     */
    public function set(
        WebauthnAttestationPolicy $body,
    ): WebauthnAttestationPolicy {
        $decoded = $this->transport->send(
            'webauthn_policy.set',
            'PUT',
            '/api/v1/tenants/{tenant_id}/webauthn/attestation-policy',
            ['tenant_id' => $this->tenantId()],
            [],
            $body->toArray(),
        );

        return WebauthnAttestationPolicy::fromArray($decoded ?? []);
    }

    /**
     * `GET /api/v1/tenants/{tenant_id}/webauthn/compliance-report`
     *
     * `GET /api/v1/tenants/{tenant_id}/webauthn/compliance-report`.
     *
     * Returns the server's complete list. This endpoint is NOT paginated, so the result is a
     * plain list and never a `Page` (§27.4 rule 4).
     * @return list<ComplianceReportEntry>
     */
    public function complianceReport(): array
    {
        $decoded = $this->transport->send(
            'webauthn_policy.compliance_report',
            'GET',
            '/api/v1/tenants/{tenant_id}/webauthn/compliance-report',
            ['tenant_id' => $this->tenantId()],
            [],
            null,
        );

        $items = [];
        foreach ($decoded ?? [] as $item) {
            if (is_array($item)) {
                $items[] = ComplianceReportEntry::fromArray($item);
            }
        }

        return $items;
    }
}
