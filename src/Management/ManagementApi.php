<?php

declare(strict_types=1);

// Generated by scripts/gen_management.py from management-registry.json and openapi.json.
// DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.

namespace Axiam\Sdk\Management;

/**
 * The CONTRACT.md §27 management surface: 146 operations across 24 namespaces.
 *
 * Reached as `$client->management()`. This class holds nothing but the shared {@see
 * ManagementTransport} and the client's default scope; each accessor below hands back a
 * namespace handle (§27.2) that can be re-scoped per call with `->inOrg()` / `->forTenant()`
 * (§27.4 rule 3).
 *
 * Handles are constructed on demand rather than cached. §27.4 rule 10 forbids caching
 * RESPONSES, not handles — but a handle is two references and a scope, and building one per
 * call keeps `->inOrg()` from having anything shared to mutate.
 */
final class ManagementApi
{
    /**
     * Wires the management surface onto the client's existing request path.
     * @param ManagementTransport $transport The one §27.8 wire path.
     * @param string|null $orgId The client's organization id, or `null` if unset.
     * @param string|null $tenantId The client's tenant id, or `null` if unset.
     */
    public function __construct(
        private readonly ManagementTransport $transport,
        private readonly ?string $orgId = null,
        private readonly ?string $tenantId = null,
    ) {
    }

    /**
     * Organizations an SDK client may read and configure. Creation and deletion are outside
     * the SDK boundary (§27.0).
     */
    public function organizations(): OrganizationsApi
    {
        return new OrganizationsApi(
            $this->transport,
            new NamespaceScope(),
            $this->orgId,
            $this->tenantId,
        );
    }

    /**
     * Tenants within an organization -- the isolation boundary every other namespace is scoped
     * to.
     */
    public function tenants(): TenantsApi
    {
        return new TenantsApi(
            $this->transport,
            new NamespaceScope(),
            $this->orgId,
            $this->tenantId,
        );
    }

    /**
     * Users within the client's tenant, and the administrative side of their second factor and
     * lockout state.
     */
    public function users(): UsersApi
    {
        return new UsersApi(
            $this->transport,
            new NamespaceScope(),
            $this->orgId,
            $this->tenantId,
        );
    }

    /**
     * Named collections of users. Roles assigned to a group are inherited by every member.
     */
    public function groups(): GroupsApi
    {
        return new GroupsApi(
            $this->transport,
            new NamespaceScope(),
            $this->orgId,
            $this->tenantId,
        );
    }

    /**
     * Roles, their permission sets, and their assignment to users and groups.
     */
    public function roles(): RolesApi
    {
        return new RolesApi(
            $this->transport,
            new NamespaceScope(),
            $this->orgId,
            $this->tenantId,
        );
    }

    /**
     * Permissions -- an action on a resource, optionally narrowed by a scope.
     */
    public function permissions(): PermissionsApi
    {
        return new PermissionsApi(
            $this->transport,
            new NamespaceScope(),
            $this->orgId,
            $this->tenantId,
        );
    }

    /**
     * The resource hierarchy role assignments cascade down.
     */
    public function resources(): ResourcesApi
    {
        return new ResourcesApi(
            $this->transport,
            new NamespaceScope(),
            $this->orgId,
            $this->tenantId,
        );
    }

    /**
     * Sub-resource granularity, always addressed under their resource.
     */
    public function scopes(): ScopesApi
    {
        return new ScopesApi(
            $this->transport,
            new NamespaceScope(),
            $this->orgId,
            $this->tenantId,
        );
    }

    /**
     * Machine identities, their secrets, and the certificate a device-bound one authenticates
     * with.
     */
    public function serviceAccounts(): ServiceAccountsApi
    {
        return new ServiceAccountsApi(
            $this->transport,
            new NamespaceScope(),
            $this->orgId,
            $this->tenantId,
        );
    }

    /**
     * End-entity X.509 certificates -- the ones issued to users, services and IoT devices.
     */
    public function certificates(): CertificatesApi
    {
        return new CertificatesApi(
            $this->transport,
            new NamespaceScope(),
            $this->orgId,
            $this->tenantId,
        );
    }

    /**
     * Organization CAs and the per-tenant signing CAs chained beneath them.
     */
    public function caCertificates(): CaCertificatesApi
    {
        return new CaCertificatesApi(
            $this->transport,
            new NamespaceScope(),
            $this->orgId,
            $this->tenantId,
        );
    }

    /**
     * OpenPGP keys used for audit signing and encrypted data export.
     */
    public function pgpKeys(): PgpKeysApi
    {
        return new PgpKeysApi(
            $this->transport,
            new NamespaceScope(),
            $this->orgId,
            $this->tenantId,
        );
    }

    /**
     * Outbound event notifications. Delivery signatures are verified with the §13 helper,
     * which this namespace configures.
     */
    public function webhooks(): WebhooksApi
    {
        return new WebhooksApi(
            $this->transport,
            new NamespaceScope(),
            $this->orgId,
            $this->tenantId,
        );
    }

    /**
     * Registered OAuth2/OIDC clients -- the registration half of what §12, §21 and §26 then
     * speak to.
     */
    public function oauth2Clients(): Oauth2ClientsApi
    {
        return new Oauth2ClientsApi(
            $this->transport,
            new NamespaceScope(),
            $this->orgId,
            $this->tenantId,
        );
    }

    /**
     * Upstream IdP configuration and the per-user links it produces.
     */
    public function federation(): FederationApi
    {
        return new FederationApi(
            $this->transport,
            new NamespaceScope(),
            $this->orgId,
            $this->tenantId,
        );
    }

    /**
     * Which events raise a notification, and to whom.
     */
    public function notificationRules(): NotificationRulesApi
    {
        return new NotificationRulesApi(
            $this->transport,
            new NamespaceScope(),
            $this->orgId,
            $this->tenantId,
        );
    }

    /**
     * Transactional-mail transport, configurable at organization level and overridable per
     * tenant.
     */
    public function emailConfig(): EmailConfigApi
    {
        return new EmailConfigApi(
            $this->transport,
            new NamespaceScope(),
            $this->orgId,
            $this->tenantId,
        );
    }

    /**
     * Effective settings, and the organization/tenant layers they resolve from.
     */
    public function settings(): SettingsApi
    {
        return new SettingsApi(
            $this->transport,
            new NamespaceScope(),
            $this->orgId,
            $this->tenantId,
        );
    }

    /**
     * Bearer tokens for the SCIM 2.0 provisioning endpoint.
     */
    public function scimTokens(): ScimTokensApi
    {
        return new ScimTokensApi(
            $this->transport,
            new NamespaceScope(),
            $this->orgId,
            $this->tenantId,
        );
    }

    /**
     * Registration of §22 AMQP extension actors -- the admin surface §22.9 describes, which no
     * SDK could previously reach.
     */
    public function reactors(): ReactorsApi
    {
        return new ReactorsApi(
            $this->transport,
            new NamespaceScope(),
            $this->orgId,
            $this->tenantId,
        );
    }

    /**
     * Per-tenant attestation policy governing the §24 ceremonies, and the compliance report
     * over it.
     */
    public function webauthnPolicy(): WebauthnPolicyApi
    {
        return new WebauthnPolicyApi(
            $this->transport,
            new NamespaceScope(),
            $this->orgId,
            $this->tenantId,
        );
    }

    /**
     * Append-only audit log, read-only by construction.
     */
    public function audit(): AuditApi
    {
        return new AuditApi(
            $this->transport,
            new NamespaceScope(),
            $this->orgId,
            $this->tenantId,
        );
    }

    /**
     * GDPR self-service: the authenticated account's own export and erasure. Scoped to the
     * caller, never to another user.
     */
    public function privacy(): PrivacyApi
    {
        return new PrivacyApi(
            $this->transport,
            new NamespaceScope(),
            $this->orgId,
            $this->tenantId,
        );
    }

    /**
     * Deployment-level probes and FIDO metadata state. Unauthenticated where the server leaves
     * them so.
     */
    public function platform(): PlatformApi
    {
        return new PlatformApi(
            $this->transport,
            new NamespaceScope(),
            $this->orgId,
            $this->tenantId,
        );
    }
}
