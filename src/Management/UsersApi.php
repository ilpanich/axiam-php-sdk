<?php

declare(strict_types=1);

// Generated by scripts/gen_management.py from management-registry.json and openapi.json.
// DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.

namespace Axiam\Sdk\Management;

use Axiam\Sdk\Management\Models\CreateUserRequest;
use Axiam\Sdk\Management\Models\MfaMethodResponse;
use Axiam\Sdk\Management\Models\RoleAssignment;
use Axiam\Sdk\Management\Models\UpdateUserRequest;
use Axiam\Sdk\Management\Models\UserResponse;

/**
 * Users within the client's tenant, and the administrative side of their second factor and
 * lockout state.
 *
 * The `users` namespace handle (CONTRACT.md §27.2), reached as
 * `$client->management()->users()`. Every method here goes through the one shared {@see
 * ManagementTransport}, so §3 CSRF, the §4 cookie jar, the §5 tenant header, §6 TLS, §16 retry
 * and §19 telemetry apply without this class doing anything to opt in (§27.8).
 */
final class UsersApi extends ManagementSupport
{
    /**
     * `GET /api/v1/users`
     *
     * `GET /api/v1/users`.
     *
     * Returns ONE page. `Page::$total` is the server's count across all pages and is not
     * `count($page)` — see §27.4 rule 4.
     * @param PageRequest|null $page which page to fetch; defaults to the first
     * @return Page<UserResponse>
     */
    public function listItems(
        ?PageRequest $page = null,
    ): Page {
        return $this->transport->sendPage(
            'users.list',
            '/api/v1/users',
            [],
            [],
            $page ?? new PageRequest(),
            static fn (array $item): UserResponse => UserResponse::fromArray($item),
        );
    }

    /**
     * `POST /api/v1/users`
     *
     * `POST /api/v1/users`.
     * @param CreateUserRequest $body the request body
     * @return UserResponse
     */
    public function create(
        CreateUserRequest $body,
    ): UserResponse {
        $decoded = $this->transport->send(
            'users.create',
            'POST',
            '/api/v1/users',
            [],
            [],
            $body->toArray(),
        );

        return UserResponse::fromArray($decoded ?? []);
    }

    /**
     * `GET /api/v1/users/{user_id}`
     *
     * `GET /api/v1/users/{user_id}`.
     * @param string $userId the `{user_id}` path parameter
     * @return UserResponse
     */
    public function get(
        string $userId,
    ): UserResponse {
        $decoded = $this->transport->send(
            'users.get',
            'GET',
            '/api/v1/users/{user_id}',
            ['user_id' => $userId],
            [],
            null,
        );

        return UserResponse::fromArray($decoded ?? []);
    }

    /**
     * `PUT /api/v1/users/{user_id}`
     *
     * `PUT /api/v1/users/{user_id}`.
     * @param string $userId the `{user_id}` path parameter
     * @param UpdateUserRequest $body the request body
     * @return UserResponse
     */
    public function update(
        string $userId,
        UpdateUserRequest $body,
    ): UserResponse {
        $decoded = $this->transport->send(
            'users.update',
            'PUT',
            '/api/v1/users/{user_id}',
            ['user_id' => $userId],
            [],
            $body->toArray(),
        );

        return UserResponse::fromArray($decoded ?? []);
    }

    /**
     * `DELETE /api/v1/users/{user_id}`
     *
     * `DELETE /api/v1/users/{user_id}`.
     *
     * Returns nothing; the server answers with an empty body.
     *
     * NOT idempotent (§27.4 rule 6): deleting something already deleted raises {@see
     * \Axiam\Sdk\Management\NotFoundError} rather than succeeding quietly.
     * @param string $userId the `{user_id}` path parameter
     */
    public function delete(
        string $userId,
    ): void {
        $this->transport->send(
            'users.delete',
            'DELETE',
            '/api/v1/users/{user_id}',
            ['user_id' => $userId],
            [],
            null,
        );
    }

    /**
     * `GET /api/v1/users/{user_id}/mfa-methods`
     *
     * `GET /api/v1/users/{user_id}/mfa-methods`.
     *
     * Returns the server's complete list. This endpoint is NOT paginated, so the result is a
     * plain list and never a `Page` (§27.4 rule 4).
     * @param string $userId the `{user_id}` path parameter
     * @return list<MfaMethodResponse>
     */
    public function listMfaMethods(
        string $userId,
    ): array {
        $decoded = $this->transport->send(
            'users.list_mfa_methods',
            'GET',
            '/api/v1/users/{user_id}/mfa-methods',
            ['user_id' => $userId],
            [],
            null,
        );

        $items = [];
        foreach ($decoded ?? [] as $item) {
            if (is_array($item)) {
                $items[] = MfaMethodResponse::fromArray($item);
            }
        }

        return $items;
    }

    /**
     * `DELETE /api/v1/users/{user_id}/mfa-methods/{method_id}`
     *
     * `DELETE /api/v1/users/{user_id}/mfa-methods/{method_id}`.
     *
     * Returns nothing; the server answers with an empty body.
     *
     * NOT idempotent (§27.4 rule 6): deleting something already deleted raises {@see
     * \Axiam\Sdk\Management\NotFoundError} rather than succeeding quietly.
     * @param string $userId the `{user_id}` path parameter
     * @param string $methodId the `{method_id}` path parameter
     */
    public function deleteMfaMethod(
        string $userId,
        string $methodId,
    ): void {
        $this->transport->send(
            'users.delete_mfa_method',
            'DELETE',
            '/api/v1/users/{user_id}/mfa-methods/{method_id}',
            ['user_id' => $userId, 'method_id' => $methodId],
            [],
            null,
        );
    }

    /**
     * `POST /api/v1/users/{user_id}/reset-mfa`
     *
     * `POST /api/v1/users/{user_id}/reset-mfa`.
     *
     * Returns nothing; the server answers with an empty body.
     * @param string $userId the `{user_id}` path parameter
     */
    public function resetMfa(
        string $userId,
    ): void {
        $this->transport->send(
            'users.reset_mfa',
            'POST',
            '/api/v1/users/{user_id}/reset-mfa',
            ['user_id' => $userId],
            [],
            null,
        );
    }

    /**
     * `POST /api/v1/users/{user_id}/unlock`
     *
     * `POST /api/v1/users/{user_id}/unlock`.
     * @param string $userId the `{user_id}` path parameter
     * @return UserResponse
     */
    public function unlock(
        string $userId,
    ): UserResponse {
        $decoded = $this->transport->send(
            'users.unlock',
            'POST',
            '/api/v1/users/{user_id}/unlock',
            ['user_id' => $userId],
            [],
            null,
        );

        return UserResponse::fromArray($decoded ?? []);
    }

    /**
     * `GET /api/v1/users/{user_id}/roles`
     *
     * `GET /api/v1/users/{user_id}/roles`.
     *
     * Returns the server's complete list. This endpoint is NOT paginated, so the result is a
     * plain list and never a `Page` (§27.4 rule 4).
     * @param string $userId the `{user_id}` path parameter
     * @return list<RoleAssignment>
     */
    public function listRoles(
        string $userId,
    ): array {
        $decoded = $this->transport->send(
            'users.list_roles',
            'GET',
            '/api/v1/users/{user_id}/roles',
            ['user_id' => $userId],
            [],
            null,
        );

        $items = [];
        foreach ($decoded ?? [] as $item) {
            if (is_array($item)) {
                $items[] = RoleAssignment::fromArray($item);
            }
        }

        return $items;
    }
}
