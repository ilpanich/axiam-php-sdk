<?php

declare(strict_types=1);

// Generated by scripts/gen_management.py from management-registry.json and openapi.json.
// DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.

namespace Axiam\Sdk\Management;

use Axiam\Sdk\Management\Models\CreateScimTokenRequest;
use Axiam\Sdk\Management\Models\CreateScimTokenResponse;
use Axiam\Sdk\Management\Models\ScimTokenResponse;

/**
 * Bearer tokens for the SCIM 2.0 provisioning endpoint.
 *
 * The `scim_tokens` namespace handle (CONTRACT.md §27.2), reached as
 * `$client->management()->scimTokens()`. Every method here goes through the one shared {@see
 * ManagementTransport}, so §3 CSRF, the §4 cookie jar, the §5 tenant header, §6 TLS, §16 retry
 * and §19 telemetry apply without this class doing anything to opt in (§27.8).
 */
final class ScimTokensApi extends ManagementSupport
{
    /**
     * `GET /api/v1/scim-tokens`
     *
     * `GET /api/v1/scim-tokens`.
     *
     * Returns the server's complete list. This endpoint is NOT paginated, so the result is a
     * plain list and never a `Page` (§27.4 rule 4).
     * @return list<ScimTokenResponse>
     */
    public function listItems(): array
    {
        $decoded = $this->transport->send(
            'scim_tokens.list',
            'GET',
            '/api/v1/scim-tokens',
            [],
            [],
            null,
        );

        $items = [];
        foreach ($decoded ?? [] as $item) {
            if (is_array($item)) {
                $items[] = ScimTokenResponse::fromArray($item);
            }
        }

        return $items;
    }

    /**
     * `POST /api/v1/scim-tokens`
     *
     * `POST /api/v1/scim-tokens`.
     *
     * The response carries a ONE-TIME secret (`provisioning_token`): the server will not
     * return it again, so a caller that does not persist it here cannot recover it (§27.5).
     * @param CreateScimTokenRequest $body the request body
     * @return CreateScimTokenResponse
     */
    public function create(
        CreateScimTokenRequest $body,
    ): CreateScimTokenResponse {
        $decoded = $this->transport->send(
            'scim_tokens.create',
            'POST',
            '/api/v1/scim-tokens',
            [],
            [],
            $body->toArray(),
        );

        return CreateScimTokenResponse::fromArray($decoded ?? []);
    }

    /**
     * `DELETE /api/v1/scim-tokens/{id}`
     *
     * `DELETE /api/v1/scim-tokens/{id}`.
     *
     * Returns nothing; the server answers with an empty body.
     *
     * NOT idempotent (§27.4 rule 6): deleting something already deleted raises {@see
     * \Axiam\Sdk\Management\NotFoundError} rather than succeeding quietly.
     * @param string $id the `{id}` path parameter
     */
    public function revoke(
        string $id,
    ): void {
        $this->transport->send(
            'scim_tokens.revoke',
            'DELETE',
            '/api/v1/scim-tokens/{id}',
            ['id' => $id],
            [],
            null,
        );
    }
}
