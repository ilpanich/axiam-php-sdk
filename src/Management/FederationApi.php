<?php

declare(strict_types=1);

// Generated by scripts/gen_management.py from management-registry.json and openapi.json.
// DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.

namespace Axiam\Sdk\Management;

use Axiam\Sdk\Management\Models\CreateFederationConfigRequest;
use Axiam\Sdk\Management\Models\FederationConfigResponse;
use Axiam\Sdk\Management\Models\FederationLinkResponse;
use Axiam\Sdk\Management\Models\OidcAuthorizeRequest;
use Axiam\Sdk\Management\Models\OidcAuthorizeResponse;
use Axiam\Sdk\Management\Models\OidcCallbackRequest;
use Axiam\Sdk\Management\Models\OidcCallbackResponse;
use Axiam\Sdk\Management\Models\UpdateFederationConfigRequest;

/**
 * Upstream IdP configuration and the per-user links it produces.
 *
 * The `federation` namespace handle (CONTRACT.md §27.2), reached as
 * `$client->management()->federation()`. Every method here goes through the one shared {@see
 * ManagementTransport}, so §3 CSRF, the §4 cookie jar, the §5 tenant header, §6 TLS, §16 retry
 * and §19 telemetry apply without this class doing anything to opt in (§27.8).
 */
final class FederationApi extends ManagementSupport
{
    /**
     * `GET /api/v1/federation-configs`
     *
     * `GET /api/v1/federation-configs`.
     *
     * Returns ONE page. `Page::$total` is the server's count across all pages and is not
     * `count($page)` — see §27.4 rule 4.
     * @param PageRequest|null $page which page to fetch; defaults to the first
     * @return Page<FederationConfigResponse>
     */
    public function listConfigs(
        ?PageRequest $page = null,
    ): Page {
        return $this->transport->sendPage(
            'federation.list_configs',
            '/api/v1/federation-configs',
            [],
            [],
            $page ?? new PageRequest(),
            static fn (array $item): FederationConfigResponse => FederationConfigResponse::fromArray($item),
        );
    }

    /**
     * `POST /api/v1/federation-configs`
     *
     * `POST /api/v1/federation-configs`.
     * @param CreateFederationConfigRequest $body the request body
     * @return FederationConfigResponse
     */
    public function createConfig(
        CreateFederationConfigRequest $body,
    ): FederationConfigResponse {
        $decoded = $this->transport->send(
            'federation.create_config',
            'POST',
            '/api/v1/federation-configs',
            [],
            [],
            $body->toArray(),
        );

        return FederationConfigResponse::fromArray($decoded ?? []);
    }

    /**
     * `GET /api/v1/federation-configs/{id}`
     *
     * `GET /api/v1/federation-configs/{id}`.
     * @param string $id the `{id}` path parameter
     * @return FederationConfigResponse
     */
    public function getConfig(
        string $id,
    ): FederationConfigResponse {
        $decoded = $this->transport->send(
            'federation.get_config',
            'GET',
            '/api/v1/federation-configs/{id}',
            ['id' => $id],
            [],
            null,
        );

        return FederationConfigResponse::fromArray($decoded ?? []);
    }

    /**
     * `PUT /api/v1/federation-configs/{id}`
     *
     * `PUT /api/v1/federation-configs/{id}`.
     * @param string $id the `{id}` path parameter
     * @param UpdateFederationConfigRequest $body the request body
     * @return FederationConfigResponse
     */
    public function updateConfig(
        string $id,
        UpdateFederationConfigRequest $body,
    ): FederationConfigResponse {
        $decoded = $this->transport->send(
            'federation.update_config',
            'PUT',
            '/api/v1/federation-configs/{id}',
            ['id' => $id],
            [],
            $body->toArray(),
        );

        return FederationConfigResponse::fromArray($decoded ?? []);
    }

    /**
     * `DELETE /api/v1/federation-configs/{id}`
     *
     * `DELETE /api/v1/federation-configs/{id}`.
     *
     * Returns nothing; the server answers with an empty body.
     *
     * NOT idempotent (§27.4 rule 6): deleting something already deleted raises {@see
     * \Axiam\Sdk\Management\NotFoundError} rather than succeeding quietly.
     * @param string $id the `{id}` path parameter
     */
    public function deleteConfig(
        string $id,
    ): void {
        $this->transport->send(
            'federation.delete_config',
            'DELETE',
            '/api/v1/federation-configs/{id}',
            ['id' => $id],
            [],
            null,
        );
    }

    /**
     * `GET /api/v1/federation-links/user/{user_id}`
     *
     * `GET /api/v1/federation-links/user/{user_id}`.
     *
     * Returns the server's complete list. This endpoint is NOT paginated, so the result is a
     * plain list and never a `Page` (§27.4 rule 4).
     * @param string $userId the `{user_id}` path parameter
     * @return list<FederationLinkResponse>
     */
    public function listUserLinks(
        string $userId,
    ): array {
        $decoded = $this->transport->send(
            'federation.list_user_links',
            'GET',
            '/api/v1/federation-links/user/{user_id}',
            ['user_id' => $userId],
            [],
            null,
        );

        $items = [];
        foreach ($decoded ?? [] as $item) {
            if (is_array($item)) {
                $items[] = FederationLinkResponse::fromArray($item);
            }
        }

        return $items;
    }

    /**
     * `DELETE /api/v1/federation-links/{id}`
     *
     * `DELETE /api/v1/federation-links/{id}`.
     *
     * Returns nothing; the server answers with an empty body.
     *
     * NOT idempotent (§27.4 rule 6): deleting something already deleted raises {@see
     * \Axiam\Sdk\Management\NotFoundError} rather than succeeding quietly.
     * @param string $id the `{id}` path parameter
     */
    public function deleteLink(
        string $id,
    ): void {
        $this->transport->send(
            'federation.delete_link',
            'DELETE',
            '/api/v1/federation-links/{id}',
            ['id' => $id],
            [],
            null,
        );
    }

    /**
     * `POST /api/v1/federation/oidc/authorize`
     *
     * `POST /api/v1/federation/oidc/authorize`.
     * @param OidcAuthorizeRequest $body the request body
     * @return OidcAuthorizeResponse
     */
    public function oidcAuthorize(
        OidcAuthorizeRequest $body,
    ): OidcAuthorizeResponse {
        $decoded = $this->transport->send(
            'federation.oidc_authorize',
            'POST',
            '/api/v1/federation/oidc/authorize',
            [],
            [],
            $body->toArray(),
        );

        return OidcAuthorizeResponse::fromArray($decoded ?? []);
    }

    /**
     * `POST /api/v1/federation/oidc/callback`
     *
     * `POST /api/v1/federation/oidc/callback`.
     * @param OidcCallbackRequest $body the request body
     * @return OidcCallbackResponse
     */
    public function oidcCallback(
        OidcCallbackRequest $body,
    ): OidcCallbackResponse {
        $decoded = $this->transport->send(
            'federation.oidc_callback',
            'POST',
            '/api/v1/federation/oidc/callback',
            [],
            [],
            $body->toArray(),
        );

        return OidcCallbackResponse::fromArray($decoded ?? []);
    }
}
