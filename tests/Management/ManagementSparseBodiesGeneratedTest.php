<?php

declare(strict_types=1);

// Generated by scripts/gen_management.py from management-registry.json and openapi.json.
// DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.

namespace Axiam\Sdk\Tests\Management;

use Axiam\Sdk\Management\Models;

/**
 * One case per SPARSE §27 request body (CONTRACT.md §27.4 rule 5).
 *
 * Each names exactly ONE field and asserts the rendered body contains that key and no other.
 * That is the whole of rule 5: an unset field is ABSENT, not null. The distinction is not
 * cosmetic — on a sparse update `null` means "erase this" and absence means "leave it alone",
 * so a serializer that emits nulls for unset fields silently wipes every field the caller did
 * not mention.
 *
 * Generated by `scripts/gen_management.py`; CI re-runs it with `--check`.
 */
final class ManagementSparseBodiesGeneratedTest extends ManagementTestCase
{
    /** `UpdateOrganizationRequest`: naming only `metadata` sends only `metadata`. */
    public function testUpdateOrganizationRequestOmitsWhatYouDidNotName(): void
    {
        $body = new Models\UpdateOrganizationRequest(metadata: []);

        $rendered = $body->toArray();

        self::assertSame(['metadata'], array_keys($rendered));
    }

    /** `UpdateTenant`: naming only `metadata` sends only `metadata`. */
    public function testUpdateTenantOmitsWhatYouDidNotName(): void
    {
        $body = new Models\UpdateTenant(metadata: []);

        $rendered = $body->toArray();

        self::assertSame(['metadata'], array_keys($rendered));
    }

    /** `UpdateUserRequest`: naming only `email` sends only `email`. */
    public function testUpdateUserRequestOmitsWhatYouDidNotName(): void
    {
        $body = new Models\UpdateUserRequest(email: 'example');

        $rendered = $body->toArray();

        self::assertSame(['email'], array_keys($rendered));
    }

    /** `UpdateGroup`: naming only `description` sends only `description`. */
    public function testUpdateGroupOmitsWhatYouDidNotName(): void
    {
        $body = new Models\UpdateGroup(description: 'example');

        $rendered = $body->toArray();

        self::assertSame(['description'], array_keys($rendered));
    }

    /** `UpdateRole`: naming only `description` sends only `description`. */
    public function testUpdateRoleOmitsWhatYouDidNotName(): void
    {
        $body = new Models\UpdateRole(description: 'example');

        $rendered = $body->toArray();

        self::assertSame(['description'], array_keys($rendered));
    }

    /** `UpdatePermissionRequest`: naming only `action` sends only `action`. */
    public function testUpdatePermissionRequestOmitsWhatYouDidNotName(): void
    {
        $body = new Models\UpdatePermissionRequest(action: 'example');

        $rendered = $body->toArray();

        self::assertSame(['action'], array_keys($rendered));
    }

    /** `UpdateResourceRequest`: naming only `metadata` sends only `metadata`. */
    public function testUpdateResourceRequestOmitsWhatYouDidNotName(): void
    {
        $body = new Models\UpdateResourceRequest(metadata: []);

        $rendered = $body->toArray();

        self::assertSame(['metadata'], array_keys($rendered));
    }

    /** `UpdateScopeRequest`: naming only `description` sends only `description`. */
    public function testUpdateScopeRequestOmitsWhatYouDidNotName(): void
    {
        $body = new Models\UpdateScopeRequest(description: 'example');

        $rendered = $body->toArray();

        self::assertSame(['description'], array_keys($rendered));
    }

    /** `UpdateServiceAccount`: naming only `description` sends only `description`. */
    public function testUpdateServiceAccountOmitsWhatYouDidNotName(): void
    {
        $body = new Models\UpdateServiceAccount(description: 'example');

        $rendered = $body->toArray();

        self::assertSame(['description'], array_keys($rendered));
    }

    /** `UpdateWebhookRequest`: naming only `enabled` sends only `enabled`. */
    public function testUpdateWebhookRequestOmitsWhatYouDidNotName(): void
    {
        $body = new Models\UpdateWebhookRequest(enabled: true);

        $rendered = $body->toArray();

        self::assertSame(['enabled'], array_keys($rendered));
    }

    /**
     * `UpdateOAuth2ClientRequest`: naming only `backchannel_logout_uri` sends only
     * `backchannel_logout_uri`.
     */
    public function testUpdateOAuth2ClientRequestOmitsWhatYouDidNotName(): void
    {
        $body = new Models\UpdateOAuth2ClientRequest(backchannelLogoutUri: 'example');

        $rendered = $body->toArray();

        self::assertSame(['backchannel_logout_uri'], array_keys($rendered));
    }

    /**
     * `UpdateFederationConfigRequest`: naming only `allowed_algorithms` sends only
     * `allowed_algorithms`.
     */
    public function testUpdateFederationConfigRequestOmitsWhatYouDidNotName(): void
    {
        $body = new Models\UpdateFederationConfigRequest(allowedAlgorithms: ['example']);

        $rendered = $body->toArray();

        self::assertSame(['allowed_algorithms'], array_keys($rendered));
    }

    /** `UpdateNotificationRuleRequest`: naming only `description` sends only `description`. */
    public function testUpdateNotificationRuleRequestOmitsWhatYouDidNotName(): void
    {
        $body = new Models\UpdateNotificationRuleRequest(description: 'example');

        $rendered = $body->toArray();

        self::assertSame(['description'], array_keys($rendered));
    }

    /** `EmailConfigOverride`: naming only `enabled` sends only `enabled`. */
    public function testEmailConfigOverrideOmitsWhatYouDidNotName(): void
    {
        $body = new Models\EmailConfigOverride(enabled: true);

        $rendered = $body->toArray();

        self::assertSame(['enabled'], array_keys($rendered));
    }

    /**
     * `TenantSettingsOverride`: naming only `access_token_lifetime_secs` sends only
     * `access_token_lifetime_secs`.
     */
    public function testTenantSettingsOverrideOmitsWhatYouDidNotName(): void
    {
        $body = new Models\TenantSettingsOverride(accessTokenLifetimeSecs: 1);

        $rendered = $body->toArray();

        self::assertSame(['access_token_lifetime_secs'], array_keys($rendered));
    }

    /** `UpdateReactorRequest`: naming only `description` sends only `description`. */
    public function testUpdateReactorRequestOmitsWhatYouDidNotName(): void
    {
        $body = new Models\UpdateReactorRequest(description: 'example');

        $rendered = $body->toArray();

        self::assertSame(['description'], array_keys($rendered));
    }

    /**
     * Every sparse body renders empty when nothing is named.
     *
     * The degenerate case of rule 5, and the one a serializer with the wrong default gets
     * wrong most visibly: a no-op update must send `{}`, not a body that nulls out every field
     * on the object.
     */
    public function testAnEmptySparseBodyRendersEmpty(): void
    {
        self::assertSame([], (new Models\UpdateOrganizationRequest())->toArray());
        self::assertSame([], (new Models\UpdateTenant())->toArray());
        self::assertSame([], (new Models\UpdateUserRequest())->toArray());
        self::assertSame([], (new Models\UpdateGroup())->toArray());
        self::assertSame([], (new Models\UpdateRole())->toArray());
        self::assertSame([], (new Models\UpdatePermissionRequest())->toArray());
        self::assertSame([], (new Models\UpdateResourceRequest())->toArray());
        self::assertSame([], (new Models\UpdateScopeRequest())->toArray());
        self::assertSame([], (new Models\UpdateServiceAccount())->toArray());
        self::assertSame([], (new Models\UpdateWebhookRequest())->toArray());
        self::assertSame([], (new Models\UpdateOAuth2ClientRequest())->toArray());
        self::assertSame([], (new Models\UpdateFederationConfigRequest())->toArray());
        self::assertSame([], (new Models\UpdateNotificationRuleRequest())->toArray());
        self::assertSame([], (new Models\EmailConfigOverride())->toArray());
        self::assertSame([], (new Models\TenantSettingsOverride())->toArray());
        self::assertSame([], (new Models\UpdateReactorRequest())->toArray());
    }
}
