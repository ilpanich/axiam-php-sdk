<?php

declare(strict_types=1);

// Generated by scripts/gen_management.py from management-registry.json and openapi.json.
// DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.

namespace Axiam\Sdk\Tests\Management;

use Axiam\Sdk\Core\Sensitive;
use Axiam\Sdk\Management\Models;
use PHPUnit\Framework\TestCase;

/**
 * One round-trip case per §27 model.
 *
 * Each decodes a wire object carrying EVERY field `openapi.json` declares, renders the result
 * back, and asserts nothing was lost. A model that can read a field but not write it again is
 * a silent one-way door -- harmless on a response, and a dropped field on anything the SDK
 * re-sends.
 *
 * Generated by `scripts/gen_management.py`; CI re-runs it with `--check`.
 */
final class ManagementModelRoundTripGeneratedTest extends TestCase
{
    /** `ActorType`: every case survives wire -> enum -> wire. */
    public function testActorTypeRoundTrips(): void
    {
        self::assertSame('User', Models\ActorType::fromWire('User')->value);
        self::assertSame('ServiceAccount', Models\ActorType::fromWire('ServiceAccount')->value);
        self::assertSame('System', Models\ActorType::fromWire('System')->value);
    }

    /** `AddMemberRequest`: a full wire object survives decode and re-render. */
    public function testAddMemberRequestRoundTrips(): void
    {
        $wire = [
            'user_id' => '11111111-1111-4111-8111-111111111111',
        ];

        $model = Models\AddMemberRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'AddMemberRequest decoded a field it cannot render again',
        );
    }

    /** `AddServiceAccountMemberRequest`: a full wire object survives decode and re-render. */
    public function testAddServiceAccountMemberRequestRoundTrips(): void
    {
        $wire = [
            'service_account_id' => '11111111-1111-4111-8111-111111111111',
        ];

        $model = Models\AddServiceAccountMemberRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'AddServiceAccountMemberRequest decoded a field it cannot render again',
        );
    }

    /** `ApiProviderConfig`: a full wire object survives decode and re-render. */
    public function testApiProviderConfigRoundTrips(): void
    {
        $wire = [
            'api_url' => 'example',
        ];

        $model = Models\ApiProviderConfig::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'ApiProviderConfig decoded a field it cannot render again',
        );
    }

    /** `AssignRoleToGroupRequest`: a full wire object survives decode and re-render. */
    public function testAssignRoleToGroupRequestRoundTrips(): void
    {
        $wire = [
            'group_id' => '11111111-1111-4111-8111-111111111111',
            'resource_id' => '11111111-1111-4111-8111-111111111111',
            'tenant_scope' => [
                '11111111-1111-4111-8111-111111111111',
            ],
        ];

        $model = Models\AssignRoleToGroupRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'AssignRoleToGroupRequest decoded a field it cannot render again',
        );
    }

    /** `AssignRoleToServiceAccountRequest`: a full wire object survives decode and re-render. */
    public function testAssignRoleToServiceAccountRequestRoundTrips(): void
    {
        $wire = [
            'resource_id' => '11111111-1111-4111-8111-111111111111',
            'service_account_id' => '11111111-1111-4111-8111-111111111111',
            'tenant_scope' => [
                '11111111-1111-4111-8111-111111111111',
            ],
        ];

        $model = Models\AssignRoleToServiceAccountRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'AssignRoleToServiceAccountRequest decoded a field it cannot render again',
        );
    }

    /** `AssignRoleToUserRequest`: a full wire object survives decode and re-render. */
    public function testAssignRoleToUserRequestRoundTrips(): void
    {
        $wire = [
            'resource_id' => '11111111-1111-4111-8111-111111111111',
            'tenant_scope' => [
                '11111111-1111-4111-8111-111111111111',
            ],
            'user_id' => '11111111-1111-4111-8111-111111111111',
        ];

        $model = Models\AssignRoleToUserRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'AssignRoleToUserRequest decoded a field it cannot render again',
        );
    }

    /** `AttestationMode`: every case survives wire -> enum -> wire. */
    public function testAttestationModeRoundTrips(): void
    {
        self::assertSame('none', Models\AttestationMode::fromWire('none')->value);
        self::assertSame('indirect', Models\AttestationMode::fromWire('indirect')->value);
        self::assertSame('direct_required', Models\AttestationMode::fromWire('direct_required')->value);
    }

    /** `AuditLogEntry`: a full wire object survives decode and re-render. */
    public function testAuditLogEntryRoundTrips(): void
    {
        $wire = [
            'action' => 'example',
            'actor_id' => '11111111-1111-4111-8111-111111111111',
            'actor_type' => 'User',
            'id' => '11111111-1111-4111-8111-111111111111',
            'ip_address' => 'example',
            'metadata' => [],
            'outcome' => 'Success',
            'resource_id' => '11111111-1111-4111-8111-111111111111',
            'tenant_id' => '11111111-1111-4111-8111-111111111111',
            'timestamp' => '2026-08-26T00:00:00Z',
        ];

        $model = Models\AuditLogEntry::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'AuditLogEntry decoded a field it cannot render again',
        );
    }

    /** `AuditOutcome`: every case survives wire -> enum -> wire. */
    public function testAuditOutcomeRoundTrips(): void
    {
        self::assertSame('Success', Models\AuditOutcome::fromWire('Success')->value);
        self::assertSame('Failure', Models\AuditOutcome::fromWire('Failure')->value);
        self::assertSame('Denied', Models\AuditOutcome::fromWire('Denied')->value);
    }

    /** `BindCertificate`: a full wire object survives decode and re-render. */
    public function testBindCertificateRoundTrips(): void
    {
        $wire = [
            'certificate_id' => '11111111-1111-4111-8111-111111111111',
        ];

        $model = Models\BindCertificate::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'BindCertificate decoded a field it cannot render again',
        );
    }

    /** `CaCertificate`: a full wire object survives decode and re-render. */
    public function testCaCertificateRoundTrips(): void
    {
        $wire = [
            'chain_pem' => 'example',
            'created_at' => '2026-08-26T00:00:00Z',
            'fingerprint' => 'example',
            'id' => '11111111-1111-4111-8111-111111111111',
            'key_algorithm' => 'Rsa4096',
            'key_custody' => 'example',
            'key_locator' => 'example',
            'mtls_trust_anchor' => true,
            'not_after' => '2026-08-26T00:00:00Z',
            'not_before' => '2026-08-26T00:00:00Z',
            'organization_id' => '11111111-1111-4111-8111-111111111111',
            'parent_ca_id' => '11111111-1111-4111-8111-111111111111',
            'public_cert_pem' => 'example',
            'status' => 'Active',
            'subject' => 'example',
            'tenant_id' => '11111111-1111-4111-8111-111111111111',
        ];

        $model = Models\CaCertificate::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'CaCertificate decoded a field it cannot render again',
        );
    }

    /** `Certificate`: a full wire object survives decode and re-render. */
    public function testCertificateRoundTrips(): void
    {
        $wire = [
            'cert_type' => 'User',
            'created_at' => '2026-08-26T00:00:00Z',
            'fingerprint' => 'example',
            'id' => '11111111-1111-4111-8111-111111111111',
            'issuer_ca_id' => '11111111-1111-4111-8111-111111111111',
            'key_algorithm' => 'Rsa4096',
            'metadata' => [],
            'not_after' => '2026-08-26T00:00:00Z',
            'not_before' => '2026-08-26T00:00:00Z',
            'public_cert_pem' => 'example',
            'status' => 'Active',
            'subject' => 'example',
            'tenant_id' => '11111111-1111-4111-8111-111111111111',
        ];

        $model = Models\Certificate::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'Certificate decoded a field it cannot render again',
        );
    }

    /** `CertificatePolicy`: a full wire object survives decode and re-render. */
    public function testCertificatePolicyRoundTrips(): void
    {
        $wire = [
            'default_cert_validity_days' => 1,
            'max_cert_validity_days' => 1,
        ];

        $model = Models\CertificatePolicy::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'CertificatePolicy decoded a field it cannot render again',
        );
    }

    /** `CertificateStatus`: every case survives wire -> enum -> wire. */
    public function testCertificateStatusRoundTrips(): void
    {
        self::assertSame('Active', Models\CertificateStatus::fromWire('Active')->value);
        self::assertSame('Revoked', Models\CertificateStatus::fromWire('Revoked')->value);
        self::assertSame('Expired', Models\CertificateStatus::fromWire('Expired')->value);
    }

    /** `CertificateType`: every case survives wire -> enum -> wire. */
    public function testCertificateTypeRoundTrips(): void
    {
        self::assertSame('User', Models\CertificateType::fromWire('User')->value);
        self::assertSame('Service', Models\CertificateType::fromWire('Service')->value);
        self::assertSame('Device', Models\CertificateType::fromWire('Device')->value);
    }

    /** `CertificationLevel`: every case survives wire -> enum -> wire. */
    public function testCertificationLevelRoundTrips(): void
    {
        self::assertSame('L1', Models\CertificationLevel::fromWire('L1')->value);
        self::assertSame('L1Plus', Models\CertificationLevel::fromWire('L1Plus')->value);
        self::assertSame('L2', Models\CertificationLevel::fromWire('L2')->value);
        self::assertSame('L2Plus', Models\CertificationLevel::fromWire('L2Plus')->value);
        self::assertSame('L3', Models\CertificationLevel::fromWire('L3')->value);
        self::assertSame('L3Plus', Models\CertificationLevel::fromWire('L3Plus')->value);
    }

    /** `ClientAuthMethod`: every case survives wire -> enum -> wire. */
    public function testClientAuthMethodRoundTrips(): void
    {
        self::assertSame('client_secret_post', Models\ClientAuthMethod::fromWire('client_secret_post')->value);
        self::assertSame('tls_client_auth', Models\ClientAuthMethod::fromWire('tls_client_auth')->value);
        self::assertSame('self_signed_tls_client_auth', Models\ClientAuthMethod::fromWire('self_signed_tls_client_auth')->value);
        self::assertSame('private_key_jwt', Models\ClientAuthMethod::fromWire('private_key_jwt')->value);
    }

    /** `ClientProfile`: every case survives wire -> enum -> wire. */
    public function testClientProfileRoundTrips(): void
    {
        self::assertSame('standard', Models\ClientProfile::fromWire('standard')->value);
        self::assertSame('fapi2', Models\ClientProfile::fromWire('fapi2')->value);
    }

    /** `ComplianceReportEntry`: a full wire object survives decode and re-render. */
    public function testComplianceReportEntryRoundTrips(): void
    {
        $wire = [
            'aaguid' => '11111111-1111-4111-8111-111111111111',
            'authenticator_name' => 'example',
            'compliant' => true,
            'credential_id' => '11111111-1111-4111-8111-111111111111',
            'name' => 'example',
            'reason' => 'example',
            'user_id' => '11111111-1111-4111-8111-111111111111',
        ];

        $model = Models\ComplianceReportEntry::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'ComplianceReportEntry decoded a field it cannot render again',
        );
    }

    /** `CreateCaCertificateRequest`: a full wire object survives decode and re-render. */
    public function testCreateCaCertificateRequestRoundTrips(): void
    {
        $wire = [
            'intermediate_subject' => 'example',
            'intermediate_validity_days' => 1,
            'issue_from_root' => true,
            'key_algorithm' => 'Rsa4096',
            'subject' => 'example',
            'validity_days' => 1,
        ];

        $model = Models\CreateCaCertificateRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'CreateCaCertificateRequest decoded a field it cannot render again',
        );
    }

    /** `CreateCertificateRequest`: a full wire object survives decode and re-render. */
    public function testCreateCertificateRequestRoundTrips(): void
    {
        $wire = [
            'cert_type' => 'User',
            'issuer_ca_id' => '11111111-1111-4111-8111-111111111111',
            'key_algorithm' => 'Rsa4096',
            'metadata' => [],
            'subject' => 'example',
            'validity_days' => 1,
        ];

        $model = Models\CreateCertificateRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'CreateCertificateRequest decoded a field it cannot render again',
        );
    }

    /** `CreateFederationConfigRequest`: a full wire object survives decode and re-render. */
    public function testCreateFederationConfigRequestRoundTrips(): void
    {
        $wire = [
            'allow_tenant_inheritance' => true,
            'allowed_algorithms' => [
                'example',
            ],
            'allowed_issuer_tenants' => [
                'example',
            ],
            'apple_key_id' => 'example',
            'apple_team_id' => 'example',
            'attribute_map' => [],
            'authorization_endpoint' => 'example',
            'button_icon' => 'example',
            'client_id' => 'example',
            'client_secret' => 'example',
            'idp_signing_cert_pem' => 'example',
            'metadata_url' => 'example',
            'protocol' => 'example',
            'provider' => 'example',
            'provider_kind' => 'example',
            'provider_slug' => 'example',
            'require_pkce' => true,
            'scopes' => [
                'example',
            ],
            'token_endpoint' => 'example',
            'token_exchange' => [
                'accepted_audiences' => [
                    'example',
                ],
                'enabled' => true,
                'max_lifetime_secs' => 1,
                'max_token_age_secs' => 1,
                'scope_map' => [],
                'subject_mapping' => 'example',
            ],
            'userinfo_endpoint' => 'example',
        ];

        $model = Models\CreateFederationConfigRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'CreateFederationConfigRequest decoded a field it cannot render again',
        );
    }

    /** `CreateGroupRequest`: a full wire object survives decode and re-render. */
    public function testCreateGroupRequestRoundTrips(): void
    {
        $wire = [
            'description' => 'example',
            'metadata' => [],
            'name' => 'example',
        ];

        $model = Models\CreateGroupRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'CreateGroupRequest decoded a field it cannot render again',
        );
    }

    /** `CreateIntermediateCaRequest`: a full wire object survives decode and re-render. */
    public function testCreateIntermediateCaRequestRoundTrips(): void
    {
        $wire = [
            'key_algorithm' => 'Rsa4096',
            'parent_ca_id' => '11111111-1111-4111-8111-111111111111',
            'subject' => 'example',
            'validity_days' => 1,
        ];

        $model = Models\CreateIntermediateCaRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'CreateIntermediateCaRequest decoded a field it cannot render again',
        );
    }

    /** `CreateNotificationRuleRequest`: a full wire object survives decode and re-render. */
    public function testCreateNotificationRuleRequestRoundTrips(): void
    {
        $wire = [
            'description' => 'example',
            'events' => [
                'login_failure',
            ],
            'name' => 'example',
            'recipient_emails' => [
                'example',
            ],
        ];

        $model = Models\CreateNotificationRuleRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'CreateNotificationRuleRequest decoded a field it cannot render again',
        );
    }

    /** `CreateOAuth2ClientRequest`: a full wire object survives decode and re-render. */
    public function testCreateOAuth2ClientRequestRoundTrips(): void
    {
        $wire = [
            'backchannel_logout_uri' => 'example',
            'dpop_bound_access_tokens' => true,
            'dpop_require_nonce' => true,
            'grant_types' => [
                'example',
            ],
            'jwks' => 'example',
            'jwks_uri' => 'example',
            'name' => 'example',
            'post_logout_redirect_uris' => [
                'example',
            ],
            'profile' => 'standard',
            'redirect_uris' => [
                'example',
            ],
            'require_par' => true,
            'scopes' => [
                'example',
            ],
            'self_signed_tls_client_auth_thumbprints' => [
                'example',
            ],
            'tls_client_auth_san_dns' => 'example',
            'tls_client_auth_san_uri' => 'example',
            'tls_client_auth_subject_dn' => 'example',
            'tls_client_certificate_bound_access_tokens' => true,
            'token_endpoint_auth_method' => 'client_secret_post',
        ];

        $model = Models\CreateOAuth2ClientRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'CreateOAuth2ClientRequest decoded a field it cannot render again',
        );
    }

    /** `CreatePermissionRequest`: a full wire object survives decode and re-render. */
    public function testCreatePermissionRequestRoundTrips(): void
    {
        $wire = [
            'action' => 'example',
            'description' => 'example',
        ];

        $model = Models\CreatePermissionRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'CreatePermissionRequest decoded a field it cannot render again',
        );
    }

    /** `CreatePgpKeyRequest`: a full wire object survives decode and re-render. */
    public function testCreatePgpKeyRequestRoundTrips(): void
    {
        $wire = [
            'algorithm' => 'Rsa4096',
            'email' => 'example',
            'name' => 'example',
            'purpose' => 'AuditSigning',
        ];

        $model = Models\CreatePgpKeyRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'CreatePgpKeyRequest decoded a field it cannot render again',
        );
    }

    /** `CreateReactorRequest`: a full wire object survives decode and re-render. */
    public function testCreateReactorRequestRoundTrips(): void
    {
        $wire = [
            'description' => 'example',
            'enabled' => true,
            'events' => [
                'example',
            ],
            'failure_policy' => 'fail_closed',
            'mode' => 'intercept',
            'name' => 'example',
            'priority' => 1,
            'timeout_ms' => 1,
        ];

        $model = Models\CreateReactorRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'CreateReactorRequest decoded a field it cannot render again',
        );
    }

    /** `CreateResourceRequest`: a full wire object survives decode and re-render. */
    public function testCreateResourceRequestRoundTrips(): void
    {
        $wire = [
            'metadata' => [],
            'name' => 'example',
            'parent_id' => '11111111-1111-4111-8111-111111111111',
            'resource_type' => 'example',
        ];

        $model = Models\CreateResourceRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'CreateResourceRequest decoded a field it cannot render again',
        );
    }

    /** `CreateRoleRequest`: a full wire object survives decode and re-render. */
    public function testCreateRoleRequestRoundTrips(): void
    {
        $wire = [
            'description' => 'example',
            'is_global' => true,
            'name' => 'example',
        ];

        $model = Models\CreateRoleRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'CreateRoleRequest decoded a field it cannot render again',
        );
    }

    /** `CreateScimTokenRequest`: a full wire object survives decode and re-render. */
    public function testCreateScimTokenRequestRoundTrips(): void
    {
        $wire = [
            'expires_in_days' => 1,
            'name' => 'example',
            'user_id' => '11111111-1111-4111-8111-111111111111',
        ];

        $model = Models\CreateScimTokenRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'CreateScimTokenRequest decoded a field it cannot render again',
        );
    }

    /** `CreateScimTokenResponse`: a full wire object survives decode and re-render. */
    public function testCreateScimTokenResponseRoundTrips(): void
    {
        $wire = [
            'created_at' => '2026-08-26T00:00:00Z',
            'created_by' => '11111111-1111-4111-8111-111111111111',
            'expires_at' => '2026-08-26T00:00:00Z',
            'id' => '11111111-1111-4111-8111-111111111111',
            'last_used_at' => '2026-08-26T00:00:00Z',
            'name' => 'example',
            'revoked_at' => '2026-08-26T00:00:00Z',
            'status' => 'active',
            'tenant_id' => '11111111-1111-4111-8111-111111111111',
            'user_id' => '11111111-1111-4111-8111-111111111111',
            'provisioning_token' => 'example',
        ];

        $model = Models\CreateScimTokenResponse::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'CreateScimTokenResponse decoded a field it cannot render again',
        );
    }

    /** `CreateScopeRequest`: a full wire object survives decode and re-render. */
    public function testCreateScopeRequestRoundTrips(): void
    {
        $wire = [
            'description' => 'example',
            'name' => 'example',
        ];

        $model = Models\CreateScopeRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'CreateScopeRequest decoded a field it cannot render again',
        );
    }

    /** `CreateServiceAccountRequest`: a full wire object survives decode and re-render. */
    public function testCreateServiceAccountRequestRoundTrips(): void
    {
        $wire = [
            'description' => 'example',
            'name' => 'example',
        ];

        $model = Models\CreateServiceAccountRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'CreateServiceAccountRequest decoded a field it cannot render again',
        );
    }

    /** `CreateTenantRequest`: a full wire object survives decode and re-render. */
    public function testCreateTenantRequestRoundTrips(): void
    {
        $wire = [
            'metadata' => [],
            'name' => 'example',
            'slug' => 'example',
        ];

        $model = Models\CreateTenantRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'CreateTenantRequest decoded a field it cannot render again',
        );
    }

    /** `CreateUserRequest`: a full wire object survives decode and re-render. */
    public function testCreateUserRequestRoundTrips(): void
    {
        $wire = [
            'email' => 'example',
            'metadata' => [],
            'opaque' => [
                'opaque_session' => 'example',
                'registration_record' => 'example',
            ],
            'password' => 'example',
            'username' => 'example',
        ];

        $model = Models\CreateUserRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'CreateUserRequest decoded a field it cannot render again',
        );
    }

    /** `CreateWebhookRequest`: a full wire object survives decode and re-render. */
    public function testCreateWebhookRequestRoundTrips(): void
    {
        $wire = [
            'events' => [
                'example',
            ],
            'retry_policy' => [
                'backoff_multiplier' => 1.5,
                'initial_delay_secs' => 1,
                'max_retries' => 1,
            ],
            'secret' => 'example',
            'url' => 'example',
        ];

        $model = Models\CreateWebhookRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'CreateWebhookRequest decoded a field it cannot render again',
        );
    }

    /** `EmailConfig`: a full wire object survives decode and re-render. */
    public function testEmailConfigRoundTrips(): void
    {
        $wire = [
            'created_at' => '2026-08-26T00:00:00Z',
            'enabled' => true,
            'from_email' => 'example',
            'from_name' => 'example',
            'id' => '11111111-1111-4111-8111-111111111111',
            'provider' => [
                'kind' => 'smtp',
                'host' => 'example',
                'port' => 1,
                'starttls' => true,
                'username' => 'example',
            ],
            'reply_to' => 'example',
            'scope' => 'Org',
            'scope_id' => '11111111-1111-4111-8111-111111111111',
            'updated_at' => '2026-08-26T00:00:00Z',
        ];

        $model = Models\EmailConfig::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'EmailConfig decoded a field it cannot render again',
        );
    }

    /** `EmailConfigOverride`: a full wire object survives decode and re-render. */
    public function testEmailConfigOverrideRoundTrips(): void
    {
        $wire = [
            'enabled' => true,
            'from_email' => 'example',
            'from_name' => 'example',
            'provider' => [
                'kind' => 'smtp',
                'host' => 'example',
                'port' => 1,
                'starttls' => true,
                'username' => 'example',
            ],
            'reply_to' => 'example',
        ];

        $model = Models\EmailConfigOverride::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'EmailConfigOverride decoded a field it cannot render again',
        );
    }

    /** `EmailTestResult`: a full wire object survives decode and re-render. */
    public function testEmailTestResultRoundTrips(): void
    {
        $wire = [
            'message_id' => 'example',
            'provider' => 'example',
            'to' => 'example',
        ];

        $model = Models\EmailTestResult::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'EmailTestResult decoded a field it cannot render again',
        );
    }

    /** `EmailVerificationPolicy`: a full wire object survives decode and re-render. */
    public function testEmailVerificationPolicyRoundTrips(): void
    {
        $wire = [
            'email_verification_grace_period_hours' => 1,
            'email_verification_required' => true,
        ];

        $model = Models\EmailVerificationPolicy::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'EmailVerificationPolicy decoded a field it cannot render again',
        );
    }

    /** `EncryptRequest`: a full wire object survives decode and re-render. */
    public function testEncryptRequestRoundTrips(): void
    {
        $wire = [
            'data_base64' => 'example',
        ];

        $model = Models\EncryptRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'EncryptRequest decoded a field it cannot render again',
        );
    }

    /** `EncryptedExport`: a full wire object survives decode and re-render. */
    public function testEncryptedExportRoundTrips(): void
    {
        $wire = [
            'ciphertext_armored' => 'example',
            'recipient_key_id' => '11111111-1111-4111-8111-111111111111',
        ];

        $model = Models\EncryptedExport::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'EncryptedExport decoded a field it cannot render again',
        );
    }

    /** `FailurePolicy`: every case survives wire -> enum -> wire. */
    public function testFailurePolicyRoundTrips(): void
    {
        self::assertSame('fail_closed', Models\FailurePolicy::fromWire('fail_closed')->value);
        self::assertSame('fail_open', Models\FailurePolicy::fromWire('fail_open')->value);
    }

    /** `FederationConfigResponse`: a full wire object survives decode and re-render. */
    public function testFederationConfigResponseRoundTrips(): void
    {
        $wire = [
            'allow_tenant_inheritance' => true,
            'allowed_algorithms' => [
                'example',
            ],
            'allowed_issuer_tenants' => [
                'example',
            ],
            'apple_key_id' => 'example',
            'apple_team_id' => 'example',
            'attribute_map' => [],
            'authorization_endpoint' => 'example',
            'button_icon' => 'example',
            'client_id' => 'example',
            'created_at' => '2026-08-26T00:00:00Z',
            'effective_scopes' => [
                'example',
            ],
            'enabled' => true,
            'has_bundled_mark' => true,
            'id' => '11111111-1111-4111-8111-111111111111',
            'metadata_url' => 'example',
            'mints_client_secret' => true,
            'pkce_required' => true,
            'protocol' => 'example',
            'provider' => 'example',
            'provider_kind' => 'example',
            'provider_slug' => 'example',
            'scopes' => [
                'example',
            ],
            'tenant_id' => '11111111-1111-4111-8111-111111111111',
            'token_endpoint' => 'example',
            'token_exchange' => [
                'accepted_audiences' => [
                    'example',
                ],
                'enabled' => true,
                'max_lifetime_secs' => 1,
                'max_token_age_secs' => 1,
                'scope_map' => [],
                'subject_mapping' => 'example',
            ],
            'updated_at' => '2026-08-26T00:00:00Z',
            'userinfo_endpoint' => 'example',
        ];

        $model = Models\FederationConfigResponse::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'FederationConfigResponse decoded a field it cannot render again',
        );
    }

    /** `FederationLinkResponse`: a full wire object survives decode and re-render. */
    public function testFederationLinkResponseRoundTrips(): void
    {
        $wire = [
            'created_at' => '2026-08-26T00:00:00Z',
            'external_email' => 'example',
            'external_subject' => 'example',
            'federation_config_id' => '11111111-1111-4111-8111-111111111111',
            'id' => '11111111-1111-4111-8111-111111111111',
            'tenant_id' => '11111111-1111-4111-8111-111111111111',
            'updated_at' => '2026-08-26T00:00:00Z',
            'user_id' => '11111111-1111-4111-8111-111111111111',
        ];

        $model = Models\FederationLinkResponse::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'FederationLinkResponse decoded a field it cannot render again',
        );
    }

    /** `GeneratedCaCertificate`: a full wire object survives decode and re-render. */
    public function testGeneratedCaCertificateRoundTrips(): void
    {
        $wire = [
            'chain_pem' => 'example',
            'created_at' => '2026-08-26T00:00:00Z',
            'fingerprint' => 'example',
            'id' => '11111111-1111-4111-8111-111111111111',
            'key_algorithm' => 'Rsa4096',
            'key_custody' => 'example',
            'key_locator' => 'example',
            'mtls_trust_anchor' => true,
            'not_after' => '2026-08-26T00:00:00Z',
            'not_before' => '2026-08-26T00:00:00Z',
            'organization_id' => '11111111-1111-4111-8111-111111111111',
            'parent_ca_id' => '11111111-1111-4111-8111-111111111111',
            'public_cert_pem' => 'example',
            'status' => 'Active',
            'subject' => 'example',
            'tenant_id' => '11111111-1111-4111-8111-111111111111',
            'private_key_pem' => 'example',
        ];

        $model = Models\GeneratedCaCertificate::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'GeneratedCaCertificate decoded a field it cannot render again',
        );
    }

    /** `GeneratedCertificate`: a full wire object survives decode and re-render. */
    public function testGeneratedCertificateRoundTrips(): void
    {
        $wire = [
            'cert_type' => 'User',
            'created_at' => '2026-08-26T00:00:00Z',
            'fingerprint' => 'example',
            'id' => '11111111-1111-4111-8111-111111111111',
            'issuer_ca_id' => '11111111-1111-4111-8111-111111111111',
            'key_algorithm' => 'Rsa4096',
            'metadata' => [],
            'not_after' => '2026-08-26T00:00:00Z',
            'not_before' => '2026-08-26T00:00:00Z',
            'public_cert_pem' => 'example',
            'status' => 'Active',
            'subject' => 'example',
            'tenant_id' => '11111111-1111-4111-8111-111111111111',
            'chain_pem' => 'example',
            'private_key_pem' => 'example',
        ];

        $model = Models\GeneratedCertificate::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'GeneratedCertificate decoded a field it cannot render again',
        );
    }

    /** `GeneratedPgpKey`: a full wire object survives decode and re-render. */
    public function testGeneratedPgpKeyRoundTrips(): void
    {
        $wire = [
            'algorithm' => 'Rsa4096',
            'created_at' => '2026-08-26T00:00:00Z',
            'fingerprint' => 'example',
            'id' => '11111111-1111-4111-8111-111111111111',
            'name' => 'example',
            'public_key_armored' => 'example',
            'purpose' => 'AuditSigning',
            'status' => 'Active',
            'tenant_id' => '11111111-1111-4111-8111-111111111111',
            'private_key_armored' => 'example',
        ];

        $model = Models\GeneratedPgpKey::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'GeneratedPgpKey decoded a field it cannot render again',
        );
    }

    /** `GrantPermissionRequest`: a full wire object survives decode and re-render. */
    public function testGrantPermissionRequestRoundTrips(): void
    {
        $wire = [
            'effect' => 'allow',
            'permission_id' => '11111111-1111-4111-8111-111111111111',
            'scope_ids' => [
                '11111111-1111-4111-8111-111111111111',
            ],
        ];

        $model = Models\GrantPermissionRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'GrantPermissionRequest decoded a field it cannot render again',
        );
    }

    /** `GrantedScope`: a full wire object survives decode and re-render. */
    public function testGrantedScopeRoundTrips(): void
    {
        $wire = [
            'id' => '11111111-1111-4111-8111-111111111111',
            'name' => 'example',
            'resource_id' => '11111111-1111-4111-8111-111111111111',
        ];

        $model = Models\GrantedScope::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'GrantedScope decoded a field it cannot render again',
        );
    }

    /** `Group`: a full wire object survives decode and re-render. */
    public function testGroupRoundTrips(): void
    {
        $wire = [
            'created_at' => '2026-08-26T00:00:00Z',
            'description' => 'example',
            'id' => '11111111-1111-4111-8111-111111111111',
            'metadata' => [],
            'name' => 'example',
            'tenant_id' => '11111111-1111-4111-8111-111111111111',
            'updated_at' => '2026-08-26T00:00:00Z',
        ];

        $model = Models\Group::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'Group decoded a field it cannot render again',
        );
    }

    /** `HealthResponse`: a full wire object survives decode and re-render. */
    public function testHealthResponseRoundTrips(): void
    {
        $wire = [
            'status' => 'example',
        ];

        $model = Models\HealthResponse::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'HealthResponse decoded a field it cannot render again',
        );
    }

    /** `ImportCaCertificateRequest`: a full wire object survives decode and re-render. */
    public function testImportCaCertificateRequestRoundTrips(): void
    {
        $wire = [
            'private_key_pem' => 'example',
            'public_cert_pem' => 'example',
        ];

        $model = Models\ImportCaCertificateRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'ImportCaCertificateRequest decoded a field it cannot render again',
        );
    }

    /** `KeyAlgorithm`: every case survives wire -> enum -> wire. */
    public function testKeyAlgorithmRoundTrips(): void
    {
        self::assertSame('Rsa4096', Models\KeyAlgorithm::fromWire('Rsa4096')->value);
        self::assertSame('Ed25519', Models\KeyAlgorithm::fromWire('Ed25519')->value);
    }

    /** `LockoutPolicy`: a full wire object survives decode and re-render. */
    public function testLockoutPolicyRoundTrips(): void
    {
        $wire = [
            'lockout_backoff_multiplier' => 1.5,
            'lockout_duration_secs' => 1,
            'max_failed_login_attempts' => 1,
            'max_lockout_duration_secs' => 1,
        ];

        $model = Models\LockoutPolicy::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'LockoutPolicy decoded a field it cannot render again',
        );
    }

    /** `MdsRefreshOutcomeInitial`: the `initial` arm decodes and re-renders. */
    public function testMdsRefreshOutcomeInitialRoundTrips(): void
    {
        $wire = [
            'outcome' => 'initial',
            'entry_count' => 1,
            'no' => 1,
        ];

        $model = Models\MdsRefreshOutcome::fromArray($wire);

        self::assertInstanceOf(Models\MdsRefreshOutcomeInitial::class, $model);
        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($model->toArray()))),
            'MdsRefreshOutcomeInitial decoded a field it cannot render again',
        );
    }

    /** `MdsRefreshOutcomeReplaced`: the `replaced` arm decodes and re-renders. */
    public function testMdsRefreshOutcomeReplacedRoundTrips(): void
    {
        $wire = [
            'outcome' => 'replaced',
            'entry_count' => 1,
            'no' => 1,
        ];

        $model = Models\MdsRefreshOutcome::fromArray($wire);

        self::assertInstanceOf(Models\MdsRefreshOutcomeReplaced::class, $model);
        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($model->toArray()))),
            'MdsRefreshOutcomeReplaced decoded a field it cannot render again',
        );
    }

    /** `MdsRefreshOutcomeNoOpRefresh`: the `no_op_refresh` arm decodes and re-renders. */
    public function testMdsRefreshOutcomeNoOpRefreshRoundTrips(): void
    {
        $wire = [
            'outcome' => 'no_op_refresh',
            'no' => 1,
        ];

        $model = Models\MdsRefreshOutcome::fromArray($wire);

        self::assertInstanceOf(Models\MdsRefreshOutcomeNoOpRefresh::class, $model);
        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($model->toArray()))),
            'MdsRefreshOutcomeNoOpRefresh decoded a field it cannot render again',
        );
    }

    /** `MdsRefreshOutcomeRollbackRejected`: the `rollback_rejected` arm decodes and re-renders. */
    public function testMdsRefreshOutcomeRollbackRejectedRoundTrips(): void
    {
        $wire = [
            'outcome' => 'rollback_rejected',
            'attempted_no' => 1,
            'stored_no' => 1,
        ];

        $model = Models\MdsRefreshOutcome::fromArray($wire);

        self::assertInstanceOf(Models\MdsRefreshOutcomeRollbackRejected::class, $model);
        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($model->toArray()))),
            'MdsRefreshOutcomeRollbackRejected decoded a field it cannot render again',
        );
    }

    /** `MdsStatusResponse`: a full wire object survives decode and re-render. */
    public function testMdsStatusResponseRoundTrips(): void
    {
        $wire = [
            'entry_count' => 1,
            'last_refreshed_at' => '2026-08-26T00:00:00Z',
            'next_update' => 'example',
            'no' => 1,
            'stale' => true,
        ];

        $model = Models\MdsStatusResponse::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'MdsStatusResponse decoded a field it cannot render again',
        );
    }

    /** `MfaMethodResponse`: a full wire object survives decode and re-render. */
    public function testMfaMethodResponseRoundTrips(): void
    {
        $wire = [
            'created_at' => 'example',
            'last_used_at' => 'example',
            'method_id' => 'example',
            'method_type' => 'Totp',
            'name' => 'example',
        ];

        $model = Models\MfaMethodResponse::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'MfaMethodResponse decoded a field it cannot render again',
        );
    }

    /** `MfaMethodType`: every case survives wire -> enum -> wire. */
    public function testMfaMethodTypeRoundTrips(): void
    {
        self::assertSame('Totp', Models\MfaMethodType::fromWire('Totp')->value);
        self::assertSame('Passkey', Models\MfaMethodType::fromWire('Passkey')->value);
        self::assertSame('SecurityKey', Models\MfaMethodType::fromWire('SecurityKey')->value);
    }

    /** `MfaPolicy`: a full wire object survives decode and re-render. */
    public function testMfaPolicyRoundTrips(): void
    {
        $wire = [
            'mfa_challenge_lifetime_secs' => 1,
            'mfa_enforced' => true,
        ];

        $model = Models\MfaPolicy::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'MfaPolicy decoded a field it cannot render again',
        );
    }

    /** `MigrateCustodyResponse`: a full wire object survives decode and re-render. */
    public function testMigrateCustodyResponseRoundTrips(): void
    {
        $wire = [
            'ca_certificate_id' => '11111111-1111-4111-8111-111111111111',
            'key_custody' => 'example',
            'key_locator' => 'example',
            'previous_custody' => 'example',
        ];

        $model = Models\MigrateCustodyResponse::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'MigrateCustodyResponse decoded a field it cannot render again',
        );
    }

    /** `MtlsTrustAnchorResponse`: a full wire object survives decode and re-render. */
    public function testMtlsTrustAnchorResponseRoundTrips(): void
    {
        $wire = [
            'ca_certificate_id' => '11111111-1111-4111-8111-111111111111',
            'message' => 'example',
            'mtls_trust_anchor' => true,
            'restart_required' => true,
            'trusted_anchors' => 1,
        ];

        $model = Models\MtlsTrustAnchorResponse::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'MtlsTrustAnchorResponse decoded a field it cannot render again',
        );
    }

    /** `NotificationEventType`: every case survives wire -> enum -> wire. */
    public function testNotificationEventTypeRoundTrips(): void
    {
        self::assertSame('login_failure', Models\NotificationEventType::fromWire('login_failure')->value);
        self::assertSame('account_locked', Models\NotificationEventType::fromWire('account_locked')->value);
        self::assertSame('mfa_enrollment_changed', Models\NotificationEventType::fromWire('mfa_enrollment_changed')->value);
        self::assertSame('password_changed', Models\NotificationEventType::fromWire('password_changed')->value);
        self::assertSame('password_reset_requested', Models\NotificationEventType::fromWire('password_reset_requested')->value);
        self::assertSame('role_assigned', Models\NotificationEventType::fromWire('role_assigned')->value);
        self::assertSame('role_unassigned', Models\NotificationEventType::fromWire('role_unassigned')->value);
        self::assertSame('permission_granted', Models\NotificationEventType::fromWire('permission_granted')->value);
        self::assertSame('permission_revoked', Models\NotificationEventType::fromWire('permission_revoked')->value);
        self::assertSame('certificate_issued', Models\NotificationEventType::fromWire('certificate_issued')->value);
        self::assertSame('certificate_revoked', Models\NotificationEventType::fromWire('certificate_revoked')->value);
        self::assertSame('ca_certificate_revoked', Models\NotificationEventType::fromWire('ca_certificate_revoked')->value);
        self::assertSame('user_created', Models\NotificationEventType::fromWire('user_created')->value);
        self::assertSame('user_deleted', Models\NotificationEventType::fromWire('user_deleted')->value);
        self::assertSame('user_updated', Models\NotificationEventType::fromWire('user_updated')->value);
        self::assertSame('service_account_created', Models\NotificationEventType::fromWire('service_account_created')->value);
        self::assertSame('service_account_deleted', Models\NotificationEventType::fromWire('service_account_deleted')->value);
    }

    /** `NotificationPolicy`: a full wire object survives decode and re-render. */
    public function testNotificationPolicyRoundTrips(): void
    {
        $wire = [
            'admin_notifications_enabled' => true,
        ];

        $model = Models\NotificationPolicy::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'NotificationPolicy decoded a field it cannot render again',
        );
    }

    /** `NotificationRuleResponse`: a full wire object survives decode and re-render. */
    public function testNotificationRuleResponseRoundTrips(): void
    {
        $wire = [
            'created_at' => '2026-08-26T00:00:00Z',
            'description' => 'example',
            'enabled' => true,
            'events' => [
                'login_failure',
            ],
            'id' => '11111111-1111-4111-8111-111111111111',
            'name' => 'example',
            'recipient_emails' => [
                'example',
            ],
            'tenant_id' => '11111111-1111-4111-8111-111111111111',
            'updated_at' => '2026-08-26T00:00:00Z',
        ];

        $model = Models\NotificationRuleResponse::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'NotificationRuleResponse decoded a field it cannot render again',
        );
    }

    /** `OAuth2ClientCreatedResponse`: a full wire object survives decode and re-render. */
    public function testOAuth2ClientCreatedResponseRoundTrips(): void
    {
        $wire = [
            'client_id' => 'example',
            'client_secret' => 'example',
            'created_at' => '2026-08-26T00:00:00Z',
            'grant_types' => [
                'example',
            ],
            'id' => '11111111-1111-4111-8111-111111111111',
            'name' => 'example',
            'redirect_uris' => [
                'example',
            ],
            'scopes' => [
                'example',
            ],
            'tenant_id' => '11111111-1111-4111-8111-111111111111',
            'updated_at' => '2026-08-26T00:00:00Z',
        ];

        $model = Models\OAuth2ClientCreatedResponse::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'OAuth2ClientCreatedResponse decoded a field it cannot render again',
        );
    }

    /** `OAuth2ClientResponse`: a full wire object survives decode and re-render. */
    public function testOAuth2ClientResponseRoundTrips(): void
    {
        $wire = [
            'client_id' => 'example',
            'created_at' => '2026-08-26T00:00:00Z',
            'dpop_bound_access_tokens' => true,
            'dpop_require_nonce' => true,
            'grant_types' => [
                'example',
            ],
            'id' => '11111111-1111-4111-8111-111111111111',
            'jwks' => 'example',
            'jwks_uri' => 'example',
            'name' => 'example',
            'profile' => 'standard',
            'redirect_uris' => [
                'example',
            ],
            'require_par' => true,
            'scopes' => [
                'example',
            ],
            'self_signed_tls_client_auth_thumbprints' => [
                'example',
            ],
            'tenant_id' => '11111111-1111-4111-8111-111111111111',
            'tls_client_auth_san_dns' => 'example',
            'tls_client_auth_san_uri' => 'example',
            'tls_client_auth_subject_dn' => 'example',
            'tls_client_certificate_bound_access_tokens' => true,
            'token_endpoint_auth_method' => 'client_secret_post',
            'updated_at' => '2026-08-26T00:00:00Z',
        ];

        $model = Models\OAuth2ClientResponse::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'OAuth2ClientResponse decoded a field it cannot render again',
        );
    }

    /** `OidcAuthorizeRequest`: a full wire object survives decode and re-render. */
    public function testOidcAuthorizeRequestRoundTrips(): void
    {
        $wire = [
            'config_id' => '11111111-1111-4111-8111-111111111111',
            'nonce' => 'example',
            'redirect_uri' => 'example',
            'state' => 'example',
        ];

        $model = Models\OidcAuthorizeRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'OidcAuthorizeRequest decoded a field it cannot render again',
        );
    }

    /** `OidcAuthorizeResponse`: a full wire object survives decode and re-render. */
    public function testOidcAuthorizeResponseRoundTrips(): void
    {
        $wire = [
            'url' => 'example',
        ];

        $model = Models\OidcAuthorizeResponse::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'OidcAuthorizeResponse decoded a field it cannot render again',
        );
    }

    /** `OidcCallbackRequest`: a full wire object survives decode and re-render. */
    public function testOidcCallbackRequestRoundTrips(): void
    {
        $wire = [
            'code' => 'example',
            'config_id' => '11111111-1111-4111-8111-111111111111',
            'nonce' => 'example',
            'redirect_uri' => 'example',
            'state' => 'example',
        ];

        $model = Models\OidcCallbackRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'OidcCallbackRequest decoded a field it cannot render again',
        );
    }

    /** `OidcCallbackResponse`: a full wire object survives decode and re-render. */
    public function testOidcCallbackResponseRoundTrips(): void
    {
        $wire = [
            'federation_link_id' => '11111111-1111-4111-8111-111111111111',
            'newly_provisioned' => true,
            'user_id' => '11111111-1111-4111-8111-111111111111',
        ];

        $model = Models\OidcCallbackResponse::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'OidcCallbackResponse decoded a field it cannot render again',
        );
    }

    /** `OpaqueEnrollment`: a full wire object survives decode and re-render. */
    public function testOpaqueEnrollmentRoundTrips(): void
    {
        $wire = [
            'opaque_session' => 'example',
            'registration_record' => 'example',
        ];

        $model = Models\OpaqueEnrollment::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'OpaqueEnrollment decoded a field it cannot render again',
        );
    }

    /** `OpaquePolicy`: a full wire object survives decode and re-render. */
    public function testOpaquePolicyRoundTrips(): void
    {
        $wire = [
            'opaque_ksf' => 'example',
            'opaque_mode' => 'example',
            'opaque_suite' => 'example',
        ];

        $model = Models\OpaquePolicy::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'OpaquePolicy decoded a field it cannot render again',
        );
    }

    /** `Organization`: a full wire object survives decode and re-render. */
    public function testOrganizationRoundTrips(): void
    {
        $wire = [
            'created_at' => '2026-08-26T00:00:00Z',
            'id' => '11111111-1111-4111-8111-111111111111',
            'metadata' => [],
            'name' => 'example',
            'slug' => 'example',
            'updated_at' => '2026-08-26T00:00:00Z',
        ];

        $model = Models\Organization::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'Organization decoded a field it cannot render again',
        );
    }

    /** `PasswordPolicy`: a full wire object survives decode and re-render. */
    public function testPasswordPolicyRoundTrips(): void
    {
        $wire = [
            'hibp_check_enabled' => true,
            'min_length' => 1,
            'password_history_count' => 1,
            'require_digits' => true,
            'require_lowercase' => true,
            'require_symbols' => true,
            'require_uppercase' => true,
        ];

        $model = Models\PasswordPolicy::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'PasswordPolicy decoded a field it cannot render again',
        );
    }

    /** `Permission`: a full wire object survives decode and re-render. */
    public function testPermissionRoundTrips(): void
    {
        $wire = [
            'action' => 'example',
            'created_at' => '2026-08-26T00:00:00Z',
            'description' => 'example',
            'id' => '11111111-1111-4111-8111-111111111111',
            'tenant_id' => '11111111-1111-4111-8111-111111111111',
            'updated_at' => '2026-08-26T00:00:00Z',
        ];

        $model = Models\Permission::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'Permission decoded a field it cannot render again',
        );
    }

    /** `PermissionEffect`: every case survives wire -> enum -> wire. */
    public function testPermissionEffectRoundTrips(): void
    {
        self::assertSame('allow', Models\PermissionEffect::fromWire('allow')->value);
        self::assertSame('deny', Models\PermissionEffect::fromWire('deny')->value);
    }

    /** `PgpKey`: a full wire object survives decode and re-render. */
    public function testPgpKeyRoundTrips(): void
    {
        $wire = [
            'algorithm' => 'Rsa4096',
            'created_at' => '2026-08-26T00:00:00Z',
            'fingerprint' => 'example',
            'id' => '11111111-1111-4111-8111-111111111111',
            'name' => 'example',
            'public_key_armored' => 'example',
            'purpose' => 'AuditSigning',
            'status' => 'Active',
            'tenant_id' => '11111111-1111-4111-8111-111111111111',
        ];

        $model = Models\PgpKey::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'PgpKey decoded a field it cannot render again',
        );
    }

    /** `PgpKeyAlgorithm`: every case survives wire -> enum -> wire. */
    public function testPgpKeyAlgorithmRoundTrips(): void
    {
        self::assertSame('Rsa4096', Models\PgpKeyAlgorithm::fromWire('Rsa4096')->value);
        self::assertSame('Ed25519', Models\PgpKeyAlgorithm::fromWire('Ed25519')->value);
    }

    /** `PgpKeyPurpose`: every case survives wire -> enum -> wire. */
    public function testPgpKeyPurposeRoundTrips(): void
    {
        self::assertSame('AuditSigning', Models\PgpKeyPurpose::fromWire('AuditSigning')->value);
        self::assertSame('Export', Models\PgpKeyPurpose::fromWire('Export')->value);
    }

    /** `PgpKeyStatus`: every case survives wire -> enum -> wire. */
    public function testPgpKeyStatusRoundTrips(): void
    {
        self::assertSame('Active', Models\PgpKeyStatus::fromWire('Active')->value);
        self::assertSame('Revoked', Models\PgpKeyStatus::fromWire('Revoked')->value);
    }

    /** `PolicyResponse`: a full wire object survives decode and re-render. */
    public function testPolicyResponseRoundTrips(): void
    {
        $wire = [
            'allowed_aaguids' => [
                '11111111-1111-4111-8111-111111111111',
            ],
            'block_revoked_status' => true,
            'blocked_aaguids' => [
                '11111111-1111-4111-8111-111111111111',
            ],
            'min_certification' => 'L1',
            'mode' => 'none',
            'require_fido_certified' => true,
            'unknown_aaguid' => 'allow',
            'effective_unknown_aaguid' => 'allow',
        ];

        $model = Models\PolicyResponse::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'PolicyResponse decoded a field it cannot render again',
        );
    }

    /** `PrivacyPolicy`: a full wire object survives decode and re-render. */
    public function testPrivacyPolicyRoundTrips(): void
    {
        $wire = [
            'deletion_grace_period_days' => 1,
        ];

        $model = Models\PrivacyPolicy::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'PrivacyPolicy decoded a field it cannot render again',
        );
    }

    /** `ProviderConfigSmtp`: the `smtp` arm decodes and re-renders. */
    public function testProviderConfigSmtpRoundTrips(): void
    {
        $wire = [
            'kind' => 'smtp',
            'host' => 'example',
            'port' => 1,
            'starttls' => true,
            'username' => 'example',
        ];

        $model = Models\ProviderConfig::fromArray($wire);

        self::assertInstanceOf(Models\ProviderConfigSmtp::class, $model);
        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($model->toArray()))),
            'ProviderConfigSmtp decoded a field it cannot render again',
        );
    }

    /** `ProviderConfigSendGrid`: the `send_grid` arm decodes and re-renders. */
    public function testProviderConfigSendGridRoundTrips(): void
    {
        $wire = [
            'kind' => 'send_grid',
            'api_url' => 'example',
        ];

        $model = Models\ProviderConfig::fromArray($wire);

        self::assertInstanceOf(Models\ProviderConfigSendGrid::class, $model);
        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($model->toArray()))),
            'ProviderConfigSendGrid decoded a field it cannot render again',
        );
    }

    /** `ProviderConfigPostmark`: the `postmark` arm decodes and re-renders. */
    public function testProviderConfigPostmarkRoundTrips(): void
    {
        $wire = [
            'kind' => 'postmark',
            'api_url' => 'example',
        ];

        $model = Models\ProviderConfig::fromArray($wire);

        self::assertInstanceOf(Models\ProviderConfigPostmark::class, $model);
        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($model->toArray()))),
            'ProviderConfigPostmark decoded a field it cannot render again',
        );
    }

    /** `ProviderConfigResend`: the `resend` arm decodes and re-renders. */
    public function testProviderConfigResendRoundTrips(): void
    {
        $wire = [
            'kind' => 'resend',
            'api_url' => 'example',
        ];

        $model = Models\ProviderConfig::fromArray($wire);

        self::assertInstanceOf(Models\ProviderConfigResend::class, $model);
        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($model->toArray()))),
            'ProviderConfigResend decoded a field it cannot render again',
        );
    }

    /** `ProviderConfigBrevo`: the `brevo` arm decodes and re-renders. */
    public function testProviderConfigBrevoRoundTrips(): void
    {
        $wire = [
            'kind' => 'brevo',
            'api_url' => 'example',
        ];

        $model = Models\ProviderConfig::fromArray($wire);

        self::assertInstanceOf(Models\ProviderConfigBrevo::class, $model);
        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($model->toArray()))),
            'ProviderConfigBrevo decoded a field it cannot render again',
        );
    }

    /** `ReactorEventDescriptor`: a full wire object survives decode and re-render. */
    public function testReactorEventDescriptorRoundTrips(): void
    {
        $wire = [
            'default_failure_policy' => 'fail_closed',
            'description' => 'example',
            'interceptable' => true,
            'mutable' => true,
            'mutable_fields' => [
                'example',
            ],
            'name' => 'example',
        ];

        $model = Models\ReactorEventDescriptor::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'ReactorEventDescriptor decoded a field it cannot render again',
        );
    }

    /** `ReactorMode`: every case survives wire -> enum -> wire. */
    public function testReactorModeRoundTrips(): void
    {
        self::assertSame('intercept', Models\ReactorMode::fromWire('intercept')->value);
        self::assertSame('listen', Models\ReactorMode::fromWire('listen')->value);
    }

    /** `ReactorResponse`: a full wire object survives decode and re-render. */
    public function testReactorResponseRoundTrips(): void
    {
        $wire = [
            'created_at' => '2026-08-26T00:00:00Z',
            'description' => 'example',
            'enabled' => true,
            'events' => [
                'example',
            ],
            'failure_policy' => 'fail_closed',
            'id' => '11111111-1111-4111-8111-111111111111',
            'last_seen_at' => '2026-08-26T00:00:00Z',
            'mode' => 'intercept',
            'name' => 'example',
            'priority' => 1,
            'recent_timeout_count' => 1,
            'recent_veto_count' => 1,
            'tenant_id' => '11111111-1111-4111-8111-111111111111',
            'timeout_ms' => 1,
            'updated_at' => '2026-08-26T00:00:00Z',
        ];

        $model = Models\ReactorResponse::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'ReactorResponse decoded a field it cannot render again',
        );
    }

    /** `ReadyResponse`: a full wire object survives decode and re-render. */
    public function testReadyResponseRoundTrips(): void
    {
        $wire = [
            'database' => 'example',
            'status' => 'example',
        ];

        $model = Models\ReadyResponse::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'ReadyResponse decoded a field it cannot render again',
        );
    }

    /** `ResolvedPermissionGrant`: a full wire object survives decode and re-render. */
    public function testResolvedPermissionGrantRoundTrips(): void
    {
        $wire = [
            'effect' => 'allow',
            'permission' => [
                'action' => 'example',
                'created_at' => '2026-08-26T00:00:00Z',
                'description' => 'example',
                'id' => '11111111-1111-4111-8111-111111111111',
                'tenant_id' => '11111111-1111-4111-8111-111111111111',
                'updated_at' => '2026-08-26T00:00:00Z',
            ],
            'scope_ids' => [
                '11111111-1111-4111-8111-111111111111',
            ],
            'scopes' => [
                [
                    'id' => '11111111-1111-4111-8111-111111111111',
                    'name' => 'example',
                    'resource_id' => '11111111-1111-4111-8111-111111111111',
                ],
            ],
        ];

        $model = Models\ResolvedPermissionGrant::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'ResolvedPermissionGrant decoded a field it cannot render again',
        );
    }

    /** `Resource`: a full wire object survives decode and re-render. */
    public function testResourceRoundTrips(): void
    {
        $wire = [
            'created_at' => '2026-08-26T00:00:00Z',
            'id' => '11111111-1111-4111-8111-111111111111',
            'metadata' => [],
            'name' => 'example',
            'parent_id' => '11111111-1111-4111-8111-111111111111',
            'resource_type' => 'example',
            'tenant_id' => '11111111-1111-4111-8111-111111111111',
            'uma_registered_by' => 'example',
            'updated_at' => '2026-08-26T00:00:00Z',
        ];

        $model = Models\Resource::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'Resource decoded a field it cannot render again',
        );
    }

    /** `RetryPolicy`: a full wire object survives decode and re-render. */
    public function testRetryPolicyRoundTrips(): void
    {
        $wire = [
            'backoff_multiplier' => 1.5,
            'initial_delay_secs' => 1,
            'max_retries' => 1,
        ];

        $model = Models\RetryPolicy::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'RetryPolicy decoded a field it cannot render again',
        );
    }

    /** `Role`: a full wire object survives decode and re-render. */
    public function testRoleRoundTrips(): void
    {
        $wire = [
            'created_at' => '2026-08-26T00:00:00Z',
            'description' => 'example',
            'id' => '11111111-1111-4111-8111-111111111111',
            'is_global' => true,
            'name' => 'example',
            'tenant_id' => '11111111-1111-4111-8111-111111111111',
            'updated_at' => '2026-08-26T00:00:00Z',
        ];

        $model = Models\Role::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'Role decoded a field it cannot render again',
        );
    }

    /** `RoleAssignment`: a full wire object survives decode and re-render. */
    public function testRoleAssignmentRoundTrips(): void
    {
        $wire = [
            'resource_id' => '11111111-1111-4111-8111-111111111111',
            'role' => [
                'created_at' => '2026-08-26T00:00:00Z',
                'description' => 'example',
                'id' => '11111111-1111-4111-8111-111111111111',
                'is_global' => true,
                'name' => 'example',
                'tenant_id' => '11111111-1111-4111-8111-111111111111',
                'updated_at' => '2026-08-26T00:00:00Z',
            ],
            'tenant_scope' => [
                '11111111-1111-4111-8111-111111111111',
            ],
        ];

        $model = Models\RoleAssignment::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'RoleAssignment decoded a field it cannot render again',
        );
    }

    /** `RoleGroupAssignment`: a full wire object survives decode and re-render. */
    public function testRoleGroupAssignmentRoundTrips(): void
    {
        $wire = [
            'group' => [
                'created_at' => '2026-08-26T00:00:00Z',
                'description' => 'example',
                'id' => '11111111-1111-4111-8111-111111111111',
                'metadata' => [],
                'name' => 'example',
                'tenant_id' => '11111111-1111-4111-8111-111111111111',
                'updated_at' => '2026-08-26T00:00:00Z',
            ],
            'resource_id' => '11111111-1111-4111-8111-111111111111',
            'tenant_scope' => [
                '11111111-1111-4111-8111-111111111111',
            ],
        ];

        $model = Models\RoleGroupAssignment::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'RoleGroupAssignment decoded a field it cannot render again',
        );
    }

    /** `RoleServiceAccountAssignment`: a full wire object survives decode and re-render. */
    public function testRoleServiceAccountAssignmentRoundTrips(): void
    {
        $wire = [
            'resource_id' => '11111111-1111-4111-8111-111111111111',
            'service_account' => [
                'client_id' => 'example',
                'created_at' => '2026-08-26T00:00:00Z',
                'description' => 'example',
                'id' => '11111111-1111-4111-8111-111111111111',
                'name' => 'example',
                'status' => 'Active',
                'tenant_id' => '11111111-1111-4111-8111-111111111111',
                'updated_at' => '2026-08-26T00:00:00Z',
            ],
            'tenant_scope' => [
                '11111111-1111-4111-8111-111111111111',
            ],
        ];

        $model = Models\RoleServiceAccountAssignment::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'RoleServiceAccountAssignment decoded a field it cannot render again',
        );
    }

    /** `RoleUserAssignment`: a full wire object survives decode and re-render. */
    public function testRoleUserAssignmentRoundTrips(): void
    {
        $wire = [
            'resource_id' => '11111111-1111-4111-8111-111111111111',
            'tenant_scope' => [
                '11111111-1111-4111-8111-111111111111',
            ],
            'user' => [
                'created_at' => '2026-08-26T00:00:00Z',
                'email' => 'example',
                'email_verified' => true,
                'failed_login_attempts' => 1,
                'id' => '11111111-1111-4111-8111-111111111111',
                'is_locked' => true,
                'locked_until' => '2026-08-26T00:00:00Z',
                'metadata' => [],
                'mfa_enabled' => true,
                'status' => 'Active',
                'tenant_id' => '11111111-1111-4111-8111-111111111111',
                'updated_at' => '2026-08-26T00:00:00Z',
                'username' => 'example',
            ],
        ];

        $model = Models\RoleUserAssignment::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'RoleUserAssignment decoded a field it cannot render again',
        );
    }

    /** `RotateSecretResponse`: a full wire object survives decode and re-render. */
    public function testRotateSecretResponseRoundTrips(): void
    {
        $wire = [
            'client_secret' => 'example',
        ];

        $model = Models\RotateSecretResponse::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'RotateSecretResponse decoded a field it cannot render again',
        );
    }

    /** `ScimTokenResponse`: a full wire object survives decode and re-render. */
    public function testScimTokenResponseRoundTrips(): void
    {
        $wire = [
            'created_at' => '2026-08-26T00:00:00Z',
            'created_by' => '11111111-1111-4111-8111-111111111111',
            'expires_at' => '2026-08-26T00:00:00Z',
            'id' => '11111111-1111-4111-8111-111111111111',
            'last_used_at' => '2026-08-26T00:00:00Z',
            'name' => 'example',
            'revoked_at' => '2026-08-26T00:00:00Z',
            'status' => 'active',
            'tenant_id' => '11111111-1111-4111-8111-111111111111',
            'user_id' => '11111111-1111-4111-8111-111111111111',
        ];

        $model = Models\ScimTokenResponse::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'ScimTokenResponse decoded a field it cannot render again',
        );
    }

    /** `ScimTokenStatus`: every case survives wire -> enum -> wire. */
    public function testScimTokenStatusRoundTrips(): void
    {
        self::assertSame('active', Models\ScimTokenStatus::fromWire('active')->value);
        self::assertSame('expired', Models\ScimTokenStatus::fromWire('expired')->value);
        self::assertSame('revoked', Models\ScimTokenStatus::fromWire('revoked')->value);
    }

    /** `Scope`: a full wire object survives decode and re-render. */
    public function testScopeRoundTrips(): void
    {
        $wire = [
            'created_at' => '2026-08-26T00:00:00Z',
            'description' => 'example',
            'id' => '11111111-1111-4111-8111-111111111111',
            'name' => 'example',
            'resource_id' => '11111111-1111-4111-8111-111111111111',
            'tenant_id' => '11111111-1111-4111-8111-111111111111',
            'updated_at' => '2026-08-26T00:00:00Z',
        ];

        $model = Models\Scope::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'Scope decoded a field it cannot render again',
        );
    }

    /** `SecuritySettings`: a full wire object survives decode and re-render. */
    public function testSecuritySettingsRoundTrips(): void
    {
        $wire = [
            'certificate' => [
                'default_cert_validity_days' => 1,
                'max_cert_validity_days' => 1,
            ],
            'created_at' => '2026-08-26T00:00:00Z',
            'email' => [
                'email_verification_grace_period_hours' => 1,
                'email_verification_required' => true,
            ],
            'id' => '11111111-1111-4111-8111-111111111111',
            'lockout' => [
                'lockout_backoff_multiplier' => 1.5,
                'lockout_duration_secs' => 1,
                'max_failed_login_attempts' => 1,
                'max_lockout_duration_secs' => 1,
            ],
            'mfa' => [
                'mfa_challenge_lifetime_secs' => 1,
                'mfa_enforced' => true,
            ],
            'notification' => [
                'admin_notifications_enabled' => true,
            ],
            'opaque' => [
                'opaque_ksf' => 'example',
                'opaque_mode' => 'example',
                'opaque_suite' => 'example',
            ],
            'password' => [
                'hibp_check_enabled' => true,
                'min_length' => 1,
                'password_history_count' => 1,
                'require_digits' => true,
                'require_lowercase' => true,
                'require_symbols' => true,
                'require_uppercase' => true,
            ],
            'privacy' => [
                'deletion_grace_period_days' => 1,
            ],
            'scope' => 'Org',
            'scope_id' => '11111111-1111-4111-8111-111111111111',
            'token' => [
                'access_token_lifetime_secs' => 1,
                'refresh_token_lifetime_secs' => 1,
            ],
            'updated_at' => '2026-08-26T00:00:00Z',
        ];

        $model = Models\SecuritySettings::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'SecuritySettings decoded a field it cannot render again',
        );
    }

    /** `ServiceAccountCreatedResponse`: a full wire object survives decode and re-render. */
    public function testServiceAccountCreatedResponseRoundTrips(): void
    {
        $wire = [
            'client_id' => 'example',
            'client_secret' => 'example',
            'created_at' => '2026-08-26T00:00:00Z',
            'description' => 'example',
            'id' => '11111111-1111-4111-8111-111111111111',
            'name' => 'example',
            'status' => 'Active',
            'tenant_id' => '11111111-1111-4111-8111-111111111111',
            'updated_at' => '2026-08-26T00:00:00Z',
        ];

        $model = Models\ServiceAccountCreatedResponse::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'ServiceAccountCreatedResponse decoded a field it cannot render again',
        );
    }

    /** `ServiceAccountResponse`: a full wire object survives decode and re-render. */
    public function testServiceAccountResponseRoundTrips(): void
    {
        $wire = [
            'client_id' => 'example',
            'created_at' => '2026-08-26T00:00:00Z',
            'description' => 'example',
            'id' => '11111111-1111-4111-8111-111111111111',
            'name' => 'example',
            'status' => 'Active',
            'tenant_id' => '11111111-1111-4111-8111-111111111111',
            'updated_at' => '2026-08-26T00:00:00Z',
        ];

        $model = Models\ServiceAccountResponse::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'ServiceAccountResponse decoded a field it cannot render again',
        );
    }

    /** `SetMtlsTrustAnchor`: a full wire object survives decode and re-render. */
    public function testSetMtlsTrustAnchorRoundTrips(): void
    {
        $wire = [
            'enabled' => true,
        ];

        $model = Models\SetMtlsTrustAnchor::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'SetMtlsTrustAnchor decoded a field it cannot render again',
        );
    }

    /** `SetOrgEmailConfig`: a full wire object survives decode and re-render. */
    public function testSetOrgEmailConfigRoundTrips(): void
    {
        $wire = [
            'enabled' => true,
            'from_email' => 'example',
            'from_name' => 'example',
            'provider' => [
                'kind' => 'smtp',
                'host' => 'example',
                'port' => 1,
                'starttls' => true,
                'username' => 'example',
            ],
            'reply_to' => 'example',
        ];

        $model = Models\SetOrgEmailConfig::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'SetOrgEmailConfig decoded a field it cannot render again',
        );
    }

    /** `SetOrgSettings`: a full wire object survives decode and re-render. */
    public function testSetOrgSettingsRoundTrips(): void
    {
        $wire = [
            'access_token_lifetime_secs' => 1,
            'admin_notifications_enabled' => true,
            'default_cert_validity_days' => 1,
            'deletion_grace_period_days' => 1,
            'email_verification_grace_period_hours' => 1,
            'email_verification_required' => true,
            'hibp_check_enabled' => true,
            'lockout_backoff_multiplier' => 1.5,
            'lockout_duration_secs' => 1,
            'max_cert_validity_days' => 1,
            'max_failed_login_attempts' => 1,
            'max_lockout_duration_secs' => 1,
            'mfa_challenge_lifetime_secs' => 1,
            'mfa_enforced' => true,
            'min_length' => 1,
            'opaque_ksf' => 'example',
            'opaque_mode' => 'example',
            'opaque_suite' => 'example',
            'password_history_count' => 1,
            'refresh_token_lifetime_secs' => 1,
            'require_digits' => true,
            'require_lowercase' => true,
            'require_symbols' => true,
            'require_uppercase' => true,
        ];

        $model = Models\SetOrgSettings::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'SetOrgSettings decoded a field it cannot render again',
        );
    }

    /** `SettingsScope`: every case survives wire -> enum -> wire. */
    public function testSettingsScopeRoundTrips(): void
    {
        self::assertSame('Org', Models\SettingsScope::fromWire('Org')->value);
        self::assertSame('Tenant', Models\SettingsScope::fromWire('Tenant')->value);
    }

    /** `SignAuditBatchRequest`: a full wire object survives decode and re-render. */
    public function testSignAuditBatchRequestRoundTrips(): void
    {
        $wire = [
            'entry_ids' => [
                '11111111-1111-4111-8111-111111111111',
            ],
        ];

        $model = Models\SignAuditBatchRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'SignAuditBatchRequest decoded a field it cannot render again',
        );
    }

    /** `SignIntermediateCsrRequest`: a full wire object survives decode and re-render. */
    public function testSignIntermediateCsrRequestRoundTrips(): void
    {
        $wire = [
            'csr_pem' => 'example',
            'parent_ca_id' => '11111111-1111-4111-8111-111111111111',
            'validity_days' => 1,
        ];

        $model = Models\SignIntermediateCsrRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'SignIntermediateCsrRequest decoded a field it cannot render again',
        );
    }

    /** `SignedAuditBatch`: a full wire object survives decode and re-render. */
    public function testSignedAuditBatchRoundTrips(): void
    {
        $wire = [
            'batch_id' => '11111111-1111-4111-8111-111111111111',
            'entry_ids' => [
                '11111111-1111-4111-8111-111111111111',
            ],
            'signature_armored' => 'example',
            'signed_at' => '2026-08-26T00:00:00Z',
            'signing_key_id' => '11111111-1111-4111-8111-111111111111',
            'tenant_id' => '11111111-1111-4111-8111-111111111111',
        ];

        $model = Models\SignedAuditBatch::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'SignedAuditBatch decoded a field it cannot render again',
        );
    }

    /** `SmtpConfig`: a full wire object survives decode and re-render. */
    public function testSmtpConfigRoundTrips(): void
    {
        $wire = [
            'host' => 'example',
            'port' => 1,
            'starttls' => true,
            'username' => 'example',
        ];

        $model = Models\SmtpConfig::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'SmtpConfig decoded a field it cannot render again',
        );
    }

    /** `Tenant`: a full wire object survives decode and re-render. */
    public function testTenantRoundTrips(): void
    {
        $wire = [
            'created_at' => '2026-08-26T00:00:00Z',
            'id' => '11111111-1111-4111-8111-111111111111',
            'kind' => 'standard',
            'metadata' => [],
            'name' => 'example',
            'organization_id' => '11111111-1111-4111-8111-111111111111',
            'slug' => 'example',
            'status' => 'Active',
            'updated_at' => '2026-08-26T00:00:00Z',
        ];

        $model = Models\Tenant::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'Tenant decoded a field it cannot render again',
        );
    }

    /** `TenantKind`: every case survives wire -> enum -> wire. */
    public function testTenantKindRoundTrips(): void
    {
        self::assertSame('standard', Models\TenantKind::fromWire('standard')->value);
        self::assertSame('organization', Models\TenantKind::fromWire('organization')->value);
    }

    /** `TenantSettingsOverride`: a full wire object survives decode and re-render. */
    public function testTenantSettingsOverrideRoundTrips(): void
    {
        $wire = [
            'access_token_lifetime_secs' => 1,
            'admin_notifications_enabled' => true,
            'default_cert_validity_days' => 1,
            'deletion_grace_period_days' => 1,
            'email_verification_grace_period_hours' => 1,
            'email_verification_required' => true,
            'hibp_check_enabled' => true,
            'lockout_backoff_multiplier' => 1.5,
            'lockout_duration_secs' => 1,
            'max_cert_validity_days' => 1,
            'max_failed_login_attempts' => 1,
            'max_lockout_duration_secs' => 1,
            'mfa_challenge_lifetime_secs' => 1,
            'mfa_enforced' => true,
            'min_length' => 1,
            'opaque_ksf' => 'example',
            'opaque_mode' => 'example',
            'opaque_suite' => 'example',
            'password_history_count' => 1,
            'refresh_token_lifetime_secs' => 1,
            'require_digits' => true,
            'require_lowercase' => true,
            'require_symbols' => true,
            'require_uppercase' => true,
        ];

        $model = Models\TenantSettingsOverride::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'TenantSettingsOverride decoded a field it cannot render again',
        );
    }

    /** `TenantStatus`: every case survives wire -> enum -> wire. */
    public function testTenantStatusRoundTrips(): void
    {
        self::assertSame('Active', Models\TenantStatus::fromWire('Active')->value);
        self::assertSame('Suspended', Models\TenantStatus::fromWire('Suspended')->value);
    }

    /** `TokenExchangeTrustRequest`: a full wire object survives decode and re-render. */
    public function testTokenExchangeTrustRequestRoundTrips(): void
    {
        $wire = [
            'accepted_audiences' => [
                'example',
            ],
            'enabled' => true,
            'max_lifetime_secs' => 1,
            'max_token_age_secs' => 1,
            'scope_map' => [],
            'subject_mapping' => 'example',
        ];

        $model = Models\TokenExchangeTrustRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'TokenExchangeTrustRequest decoded a field it cannot render again',
        );
    }

    /** `TokenExchangeTrustResponse`: a full wire object survives decode and re-render. */
    public function testTokenExchangeTrustResponseRoundTrips(): void
    {
        $wire = [
            'accepted_audiences' => [
                'example',
            ],
            'enabled' => true,
            'max_lifetime_secs' => 1,
            'max_token_age_secs' => 1,
            'scope_map' => [],
            'subject_mapping' => 'example',
        ];

        $model = Models\TokenExchangeTrustResponse::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'TokenExchangeTrustResponse decoded a field it cannot render again',
        );
    }

    /** `TokenPolicy`: a full wire object survives decode and re-render. */
    public function testTokenPolicyRoundTrips(): void
    {
        $wire = [
            'access_token_lifetime_secs' => 1,
            'refresh_token_lifetime_secs' => 1,
        ];

        $model = Models\TokenPolicy::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'TokenPolicy decoded a field it cannot render again',
        );
    }

    /** `UnknownAaguidAction`: every case survives wire -> enum -> wire. */
    public function testUnknownAaguidActionRoundTrips(): void
    {
        self::assertSame('allow', Models\UnknownAaguidAction::fromWire('allow')->value);
        self::assertSame('deny', Models\UnknownAaguidAction::fromWire('deny')->value);
    }

    /** `UpdateFederationConfigRequest`: a full wire object survives decode and re-render. */
    public function testUpdateFederationConfigRequestRoundTrips(): void
    {
        $wire = [
            'allow_tenant_inheritance' => true,
            'allowed_algorithms' => [
                'example',
            ],
            'allowed_issuer_tenants' => [
                'example',
            ],
            'apple_key_id' => 'example',
            'apple_team_id' => 'example',
            'attribute_map' => [],
            'authorization_endpoint' => 'example',
            'button_icon' => 'example',
            'client_id' => 'example',
            'client_secret' => 'example',
            'enabled' => true,
            'idp_signing_cert_pem' => 'example',
            'metadata_url' => 'example',
            'provider' => 'example',
            'provider_slug' => 'example',
            'require_pkce' => true,
            'scopes' => [
                'example',
            ],
            'token_endpoint' => 'example',
            'token_exchange' => [
                'accepted_audiences' => [
                    'example',
                ],
                'enabled' => true,
                'max_lifetime_secs' => 1,
                'max_token_age_secs' => 1,
                'scope_map' => [],
                'subject_mapping' => 'example',
            ],
            'userinfo_endpoint' => 'example',
        ];

        $model = Models\UpdateFederationConfigRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'UpdateFederationConfigRequest decoded a field it cannot render again',
        );
    }

    /** `UpdateGroup`: a full wire object survives decode and re-render. */
    public function testUpdateGroupRoundTrips(): void
    {
        $wire = [
            'description' => 'example',
            'metadata' => [],
            'name' => 'example',
        ];

        $model = Models\UpdateGroup::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'UpdateGroup decoded a field it cannot render again',
        );
    }

    /** `UpdateNotificationRuleRequest`: a full wire object survives decode and re-render. */
    public function testUpdateNotificationRuleRequestRoundTrips(): void
    {
        $wire = [
            'description' => 'example',
            'enabled' => true,
            'events' => [
                'login_failure',
            ],
            'name' => 'example',
            'recipient_emails' => [
                'example',
            ],
        ];

        $model = Models\UpdateNotificationRuleRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'UpdateNotificationRuleRequest decoded a field it cannot render again',
        );
    }

    /** `UpdateOAuth2ClientRequest`: a full wire object survives decode and re-render. */
    public function testUpdateOAuth2ClientRequestRoundTrips(): void
    {
        $wire = [
            'backchannel_logout_uri' => 'example',
            'dpop_bound_access_tokens' => true,
            'dpop_require_nonce' => true,
            'grant_types' => [
                'example',
            ],
            'jwks' => 'example',
            'jwks_uri' => 'example',
            'name' => 'example',
            'post_logout_redirect_uris' => [
                'example',
            ],
            'profile' => 'standard',
            'redirect_uris' => [
                'example',
            ],
            'require_par' => true,
            'scopes' => [
                'example',
            ],
            'self_signed_tls_client_auth_thumbprints' => [
                'example',
            ],
            'tls_client_auth_san_dns' => 'example',
            'tls_client_auth_san_uri' => 'example',
            'tls_client_auth_subject_dn' => 'example',
            'tls_client_certificate_bound_access_tokens' => true,
            'token_endpoint_auth_method' => 'client_secret_post',
        ];

        $model = Models\UpdateOAuth2ClientRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'UpdateOAuth2ClientRequest decoded a field it cannot render again',
        );
    }

    /** `UpdateOrganizationRequest`: a full wire object survives decode and re-render. */
    public function testUpdateOrganizationRequestRoundTrips(): void
    {
        $wire = [
            'metadata' => [],
            'name' => 'example',
            'slug' => 'example',
        ];

        $model = Models\UpdateOrganizationRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'UpdateOrganizationRequest decoded a field it cannot render again',
        );
    }

    /** `UpdatePermissionRequest`: a full wire object survives decode and re-render. */
    public function testUpdatePermissionRequestRoundTrips(): void
    {
        $wire = [
            'action' => 'example',
            'description' => 'example',
        ];

        $model = Models\UpdatePermissionRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'UpdatePermissionRequest decoded a field it cannot render again',
        );
    }

    /** `UpdateReactorRequest`: a full wire object survives decode and re-render. */
    public function testUpdateReactorRequestRoundTrips(): void
    {
        $wire = [
            'description' => 'example',
            'enabled' => true,
            'events' => [
                'example',
            ],
            'failure_policy' => 'fail_closed',
            'mode' => 'intercept',
            'name' => 'example',
            'priority' => 1,
            'timeout_ms' => 1,
        ];

        $model = Models\UpdateReactorRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'UpdateReactorRequest decoded a field it cannot render again',
        );
    }

    /** `UpdateResourceRequest`: a full wire object survives decode and re-render. */
    public function testUpdateResourceRequestRoundTrips(): void
    {
        $wire = [
            'metadata' => [],
            'name' => 'example',
            'parent_id' => '11111111-1111-4111-8111-111111111111',
            'resource_type' => 'example',
        ];

        $model = Models\UpdateResourceRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'UpdateResourceRequest decoded a field it cannot render again',
        );
    }

    /** `UpdateRole`: a full wire object survives decode and re-render. */
    public function testUpdateRoleRoundTrips(): void
    {
        $wire = [
            'description' => 'example',
            'is_global' => true,
            'name' => 'example',
        ];

        $model = Models\UpdateRole::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'UpdateRole decoded a field it cannot render again',
        );
    }

    /** `UpdateScopeRequest`: a full wire object survives decode and re-render. */
    public function testUpdateScopeRequestRoundTrips(): void
    {
        $wire = [
            'description' => 'example',
            'name' => 'example',
        ];

        $model = Models\UpdateScopeRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'UpdateScopeRequest decoded a field it cannot render again',
        );
    }

    /** `UpdateServiceAccount`: a full wire object survives decode and re-render. */
    public function testUpdateServiceAccountRoundTrips(): void
    {
        $wire = [
            'description' => 'example',
            'name' => 'example',
            'status' => 'Active',
        ];

        $model = Models\UpdateServiceAccount::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'UpdateServiceAccount decoded a field it cannot render again',
        );
    }

    /** `UpdateTenant`: a full wire object survives decode and re-render. */
    public function testUpdateTenantRoundTrips(): void
    {
        $wire = [
            'metadata' => [],
            'name' => 'example',
            'slug' => 'example',
            'status' => 'Active',
        ];

        $model = Models\UpdateTenant::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'UpdateTenant decoded a field it cannot render again',
        );
    }

    /** `UpdateUserRequest`: a full wire object survives decode and re-render. */
    public function testUpdateUserRequestRoundTrips(): void
    {
        $wire = [
            'email' => 'example',
            'metadata' => [],
            'status' => 'Active',
            'username' => 'example',
        ];

        $model = Models\UpdateUserRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'UpdateUserRequest decoded a field it cannot render again',
        );
    }

    /** `UpdateWebhookRequest`: a full wire object survives decode and re-render. */
    public function testUpdateWebhookRequestRoundTrips(): void
    {
        $wire = [
            'enabled' => true,
            'events' => [
                'example',
            ],
            'retry_policy' => [
                'backoff_multiplier' => 1.5,
                'initial_delay_secs' => 1,
                'max_retries' => 1,
            ],
            'secret' => 'example',
            'url' => 'example',
        ];

        $model = Models\UpdateWebhookRequest::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'UpdateWebhookRequest decoded a field it cannot render again',
        );
    }

    /** `UserResponse`: a full wire object survives decode and re-render. */
    public function testUserResponseRoundTrips(): void
    {
        $wire = [
            'created_at' => '2026-08-26T00:00:00Z',
            'email' => 'example',
            'email_verified' => true,
            'failed_login_attempts' => 1,
            'id' => '11111111-1111-4111-8111-111111111111',
            'is_locked' => true,
            'locked_until' => '2026-08-26T00:00:00Z',
            'metadata' => [],
            'mfa_enabled' => true,
            'status' => 'Active',
            'tenant_id' => '11111111-1111-4111-8111-111111111111',
            'updated_at' => '2026-08-26T00:00:00Z',
            'username' => 'example',
        ];

        $model = Models\UserResponse::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'UserResponse decoded a field it cannot render again',
        );
    }

    /** `UserStatus`: every case survives wire -> enum -> wire. */
    public function testUserStatusRoundTrips(): void
    {
        self::assertSame('Active', Models\UserStatus::fromWire('Active')->value);
        self::assertSame('Inactive', Models\UserStatus::fromWire('Inactive')->value);
        self::assertSame('Locked', Models\UserStatus::fromWire('Locked')->value);
        self::assertSame('PendingVerification', Models\UserStatus::fromWire('PendingVerification')->value);
        self::assertSame('Anonymized', Models\UserStatus::fromWire('Anonymized')->value);
        self::assertSame('Deleted', Models\UserStatus::fromWire('Deleted')->value);
    }

    /** `WebauthnAttestationPolicy`: a full wire object survives decode and re-render. */
    public function testWebauthnAttestationPolicyRoundTrips(): void
    {
        $wire = [
            'allowed_aaguids' => [
                '11111111-1111-4111-8111-111111111111',
            ],
            'block_revoked_status' => true,
            'blocked_aaguids' => [
                '11111111-1111-4111-8111-111111111111',
            ],
            'min_certification' => 'L1',
            'mode' => 'none',
            'require_fido_certified' => true,
            'unknown_aaguid' => 'allow',
        ];

        $model = Models\WebauthnAttestationPolicy::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'WebauthnAttestationPolicy decoded a field it cannot render again',
        );
    }

    /** `WebhookResponse`: a full wire object survives decode and re-render. */
    public function testWebhookResponseRoundTrips(): void
    {
        $wire = [
            'created_at' => '2026-08-26T00:00:00Z',
            'enabled' => true,
            'events' => [
                'example',
            ],
            'id' => '11111111-1111-4111-8111-111111111111',
            'retry_policy' => [
                'backoff_multiplier' => 1.5,
                'initial_delay_secs' => 1,
                'max_retries' => 1,
            ],
            'tenant_id' => '11111111-1111-4111-8111-111111111111',
            'updated_at' => '2026-08-26T00:00:00Z',
            'url' => 'example',
        ];

        $model = Models\WebhookResponse::fromArray($wire);
        $rendered = $model->toArray();

        self::assertSame(
            [],
            array_values(array_diff(array_keys($wire), array_keys($rendered))),
            'WebhookResponse decoded a field it cannot render again',
        );
    }

    /**
     * Every generated model has a round-trip case.
     *
     * Counted reflectively, so a model dropped by a bad regeneration fails here rather than
     * quietly reducing what is checked.
     */
    public function testEveryModelHasARoundTripCase(): void
    {
        $reflection = new \ReflectionClass(self::class);
        $cases = array_filter(
            $reflection->getMethods(\ReflectionMethod::IS_PUBLIC),
            static fn (\ReflectionMethod $m): bool => str_ends_with($m->getName(), 'RoundTrips'),
        );

        self::assertCount(147, $cases);
    }
}
